# Instruction / evidence graph -- the AUTHORED JOIN only.
# Derived at check time, never copied here: the 69 rules and their text
# (chemsmart.agent.rules), the code gates, the capability ladder, word and
# token costs (loop/census.py).
# A node points at evidence; it never restates it. `anchor` is a verbatim
# single-line substring, so a moved or reworded source turns graphcheck red.
version: 1

nodes:
  - id: fundamental.hub
    kind: fundamental
    source: {path: CONDUCT.md, anchor: "one public"}
    note: No model-authored native input, path, shell or status; one YAML-and-CLI layer.
  - id: fundamental.researcher
    kind: fundamental
    source: {path: CONDUCT.md, anchor: "The anomaly has standing."}
    note: Honesty, provenance, freedom of route, anomalies as evidence.
  - id: invariant.gates
    kind: invariant
    source: {path: CONDUCT.md, anchor: "A refusal lives in code only when it protects an invariant that"}
    note: The closed list of what may be a code gate; everything else is a sentence at the point of use.
  - id: invariant.claim_ladder
    kind: invariant
    source: {path: CONDUCT.md, anchor: "The claim ladder -- proposed, planned, materialised, previewed,"}
    duplicates: [AGENTS.md "Scientific invariants", .agents/skills/chemsmart-agent/SKILL.md]

  # Charter sections, classified. Words come from census.py, not from here.
  - {id: charter.mission, kind: charter_section, class: FUNDAMENTAL, source: {path: AGENTS.md, anchor: "## Mission"}}
  - {id: charter.architecture, kind: charter_section, class: HARD_INVARIANT, source: {path: AGENTS.md, anchor: "## Architecture"}, consumed_by: [tests/agent/test_the_architecture_diagram_names_real_symbols.py]}
  - {id: charter.product_boundary, kind: charter_section, class: BOUNDARY_FACT, source: {path: AGENTS.md, anchor: "## Product boundary for version 3.1.4"}, derivable_from: "chemsmart agent capabilities --json", note: "kernel keeps the supports list only; the per-surface narrative was relocated verbatim to .agents/charter/ (e8dd7a50) and is reachable as topic:<slug>"}
  - {id: charter.authority_chain, kind: charter_section, class: HARD_INVARIANT, source: {path: AGENTS.md, anchor: "## Authority and approval chain"}, note: "kernel keeps the seven steps; the rest was relocated verbatim to .agents/charter/ (e8dd7a50)"}
  - {id: charter.scientific_invariants, kind: charter_section, class: FUNDAMENTAL, source: {path: AGENTS.md, anchor: "## Scientific invariants"}}
  - {id: charter.implementation_discipline, kind: charter_section, class: PROCEDURE, source: {path: AGENTS.md, anchor: "## Implementation discipline"}}

  # Lessons the laboratory paid for. Each points at its source; none restates it.
  - id: lesson.computed_not_consumed
    kind: lesson
    source: {path: CONDUCT.md, anchor: "the mechanism is right where it is computed"}
    revalidate: every audit
    falsifier: An audit round finds no mechanism whose value is computed and then read by nothing.
  - id: lesson.refusal_teaches
    kind: lesson
    source: {path: CONDUCT.md, anchor: "the refusal message, not the guide, is where"}
    falsifier: Routed refusals show no change in retry or recovery against unrouted ones on matched tasks.
  - id: lesson.refusals_that_grade_chemistry
    kind: lesson
    source: {path: MAINTENANCE.md, anchor: "Refusals that graded chemistry"}
    falsifier: A refusal on scientific merit prevents a wrong delivery that an observation would have let through.
  - id: lesson.witness_must_be_red
    kind: lesson
    source: {path: CONDUCT.md, anchor: "a witness that was never red witnesses"}
    consumed_by: [loop/graph.py]
  - id: lesson.defect_as_probe
    kind: lesson
    source: {path: CONDUCT.md, anchor: "as a probe, not a ticket"}
  - id: lesson.explanation_is_not_authority
    kind: lesson
    source: {path: CONDUCT.md, anchor: "an explanation string never confers authority"}
  - id: lesson.record_is_not_grant
    kind: lesson
    source: {path: CONDUCT.md, anchor: "a record's existence is not a"}
  - id: lesson.survive_vs_preserve
    kind: lesson
    source: {path: CONDUCT.md, anchor: "surviving an error and preserving what"}
  - id: lesson.check_physics_before_code
    kind: lesson
    source: {path: CONDUCT.md, anchor: "were wrong tests"}
  - id: lesson.task_text_never_asks
    kind: lesson
    source: {path: CONDUCT.md, anchor: "the task text never asks"}
  - id: lesson.no_reroll
    kind: lesson
    source: {path: CONDUCT.md, anchor: "weak run is never"}
  - id: lesson.small_cases_find_defects
    kind: lesson
    source: {path: MAINTENANCE.md, anchor: "found two host defects"}
    falsifier: A long window finds a host defect class that three unrelated four-atom cases could not reach.
  - id: lesson.writer_reader_two_tables
    kind: lesson
    source: {path: MAINTENANCE.md, anchor: "A writer and a reader are two tables"}

  # Results that came back negative, and the commits that replaced a sentence.
  - id: result.xtb_mode_tables_agree
    kind: negative_result
    commit: "080535cd"
    note: Seven real xTB Hessians - vibspectrum and g98.out agree row for row (max 0.0050 cm^-1). The guard built for a disagreement was declined; the sealing was absorbed (ec1f5ba5).
    falsifier: One real xTB result whose two native tables disagree in order, count or frequency beyond print rounding.
  - id: commit.44499f2a
    kind: commit
    commit: "44499f2a"
    note: pyscf - a Hessian of any surface the driver can differentiate (finite difference of the analytic gradient).
  - id: commit.ec1f5ba5
    kind: commit
    commit: "ec1f5ba5"
    note: xTB mode-table sealing, absorbed selectively from 080535cd.
  - id: result.pak_campaign_pyscf_20260919
    kind: result
    source: {path: .agents/charter/pyscf.md, anchor: "A sealed four-goal campaign ran that tree on CUHK (jobs 2140018-2140021,"}
    note: "Four sealed goals, four observations, no arm without the sentences. The root's Hessian was planned in both excited-state goals and ran in one (all modes real); stability was asked on the twisted ethylene and on ozone's RHF reference, and of a closed-shell ground state only where ethylene's planar optimisation shared the twisted structure's project; the MP2 Hessian boundary was quoted and held."
    falsifier: "A sealed goal on an excited-state equilibrium structure plans no Hessian on the root, or one on the ground state; or a session delivers DFT frequencies as MP2."

  - id: negative.geometric_kernel_irc_never_steps
    kind: negative_result
    source: {path: .agents/charter/pyscf.md, anchor: "not reach the first step under PySCF 2.14 and geomeTRIC 1.1.1 --"}
    note: "pyscf.geomopt.geometric_solver.kernel(mf, irc=True) raises AttributeError ('super' object has no attribute 'molecules') before any gradient: run_optimizer reads M.molecules for every IRC and PySCF's engine never builds the topology. The TS search does not meet the line because it is guarded by params.irc."
    falsifier: "Under the pinned PySCF and geomeTRIC, kernel(mf, irc=True) takes a first IRC step from a first-order saddle."
  - id: negative.geometric_forward_is_a_sign
    kind: negative_result
    source: {path: .agents/charter/pyscf.md, anchor: "sign, not a species: geomeTRIC's ``forward`` steps against the eigenvector it"}
    note: "Measured on the formaldehyde 1,2-H shift: with the eigenvector geomeTRIC holds made canonical, its 'forward' first step projected -0.756 on it and 'backward' +0.742; the host now hands geomeTRIC the negated vector and measures the projection from the recorded frames."
    falsifier: "geomeTRIC's first IRC step for 'forward' is observed along +TSNormal_modes_x[0]."
  - id: result.irc_path_is_not_everywhere_steepest_descent
    kind: result
    source: {path: .agents/charter/pyscf.md, anchor: "shift at HF/6-31G* the median cosine is 0.999 while single mid-path steps fall"}
    note: "Two implementations agree (the validator and reference_irc.py): per-step cosines with the mean unit negative mass-weighted gradient, median 0.999, minima 0.60 and 0.18 mid-path and -0.15 among the last steps at a minimum. Stated, never graded."
    falsifier: "Recomputing the cosines from an archived IRC's frames and gradients disagrees with the validator's, or a finer-step walk of the same surface reaches a different basin."
  - id: result.opt_final_scf_started_from_the_input_density
    kind: result
    source: {path: .agents/charter/pyscf.md, anchor: "geometry's density while its record said ``final_scf_from_optimizer_density:"}
    note: "PySCF's gradient scanner is a copy of the mean field; mf.mo_coeff is untouched by the walk, and the archived water_opt final SCF took six cycles where a restart from the walk's density takes one."
    falsifier: "An optimisation's final SCF, run by a driver before this round, took as few cycles as a restart from the walk's last density."

  - id: result.pyscf_irc_campaign_20260920
    kind: result
    source: {path: .agents/charter/pyscf.md, anchor: "Five sealed goals ran the IRC tree on CUHK (jobs 2140676, 2140679-2140682;"}
    note: "Five sealed goals, 20 of 32 engine calls. Vinyl alcohol, HONO (ORCA saddle, PySCF branches) and H2CO -> H2 + CO delivered barriers within 0.01 kcal/mol of the direct-CLI references; every walked branch was read for where it ended before it was named. Ethane (a minimum called a TS) was recognised before any engine and settled exhausted; methoxy (a saddle from another surface) declined the IRC, found the true minima by displacement, withdrew its biased barriers and was returned. Twice the session inverted which barrier a more stable minimum carries; the host's expectation rows caught both."
    falsifier: "A sealed goal names the minima a saddle connects from the branch word or from one branch, or delivers an IRC endpoint reached from a saddle of another surface as a minimum of this one."
  - id: result.a_last_cycle_wave_was_promised_a_wake
    kind: negative_result
    source: {path: .agents/charter/goal-grain-recovery-wake.md, anchor: "woken once, when all of them have ended\": a woken cycle's plan is admitted"}
    note: "g3-ethane and g5-methoxy each selected a wave in their last cycle, were told they would be woken, deferred claims to that wake and were settled without it (exhausted; returned_to_human with four numbers unclaimed). Every goal context now states wakes_after_this_cycle."
    falsifier: "A goal context's wakes_after_this_cycle differs from the number of further cycles the goal loop opens after that cycle's run."
  - id: result.a_result_file_is_not_a_structure_edge
    kind: negative_result
    source: {path: .agents/charter/goal-grain-recovery-wake.md, anchor: "A node fed its producer's result"}
    note: "g3-ethane bound Hessian and IRC nodes to an optimisation's pyscf_hdf5; none could be previewed or deferred and its first cycle ran nothing while every reply named the routes for a node lacking a preview. The blocking node now carries a blocking_reason naming geometry_xyz."
    falsifier: "A consumer bound to a structure producer's result file is deferred and runs inside one approval, or blocks with no reason naming the structure edge."
  - id: negative.mode_count_certified_a_non_stationary_saddle
    kind: negative_result
    source: {path: .agents/charter/validity-rules.md, anchor: "The mode count is not stationarity, and one organ read it as such."}
    note: "characterise_stationary_point issued order 1 for a Hessian taken at 0.0485 Eh/Bohr, with the host's own gradient anomaly uncited. Its own falsifier fired on 2026-09-21: the certification now refuses a measured gradient above the criterion, naming both numbers and the routes that remain, and the archived water_stretched_hess -- which this organ certified as 'a minimum' at 0.0185 Eh/Bohr -- is refused with it. Closed for the programs whose reader can bind a gradient to one structure; ORCA and Gaussian serve a gradient per optimisation step and answer nothing, which the receipt records as unmeasured rather than as stationarity."
    falsifier: "characterise_stationary_point refuses, or cites the gradient anomaly for, a result whose stationarity gradient exceeds the policy criterion."

  - id: negative.geometric_updated_hessian_is_not_an_order
    kind: negative_result
    source: {path: ".agents/charter/pyscf.md", anchor: "geomeTRIC's own updated curvature at the end was recorded for one revision"}
    note: "geomeTRIC's updated (quasi-Newton) Hessian at a converged P-RFO transition-state search is in the optimiser's internal coordinates and unprojected, so its negative-eigenvalue count is not the order of the structure: on the H2CO/HCOH saddle the PySCF ts stage located to 0.0001 A of ORCA's -- a genuine first-order saddle by an analytic Hessian at -2700.0 cm-1 -- that count was four. The number is deliberately not recorded."
    falsifier: "On a set of converged searches whose analytic Hessians give a known order, geomeTRIC's updated-Hessian negative-eigenvalue count matches that order."

  - id: negative.a_converged_ts_search_can_be_a_minimum
    kind: negative_result
    source: {path: ".agents/charter/pyscf.md", anchor: "a search seeded at a *minimum* of its own surface converges in one iteration"}
    note: "A PySCF ts node seeded at a minimum of its own surface converges in one iteration, moves 0.007 amu^1/2 bohr and delivers that minimum under a validated receipt (archived h2co_ts_from_minimum, CUHK 2141124). 'Converged' is a statement about the gradient and the step. No host rule types it: the artifact prints no spectrum, so it makes no claim, and a number delivered from it is worded uncharacterised until a hess node on its geometry joins it."
    falsifier: "A ts artifact carries a spectrum of the structure it reached, so the program-neutral order rule can judge the node in-run."

  - id: result.pyscf_ts_cli_20260921
    kind: result
    source: {path: ".agents/charter/pyscf.md", anchor: "The direct CLI evidence (CUHK Slurm 2141124, four minutes)"}
    note: "Two programs' saddle searches on one surface agree without sharing an optimiser or an initial Hessian: PySCF's new ts stage reaches ORCA's OptTS H2CO/trans-HCOH saddle to 1e-4 A in every interatomic distance from a seed 0.24 A away carrying two imaginary modes, and HCN/HNC to 3e-4 A. The Hessian at what it reached gives -2700.0 cm-1, the frequency the archived IRC branches start from, and both branches walked from it reach the two minima. A four-goal sealed campaign then ran the same tree (2141227-2141231): all four goals planned a PySCF ts node from ordinary language and executed one, six searches over four molecules, all validated, none settling achieved."
    falsifier: "A PySCF ts and an ORCA OptTS on one surface from one seed reach structures differing by more than optimiser convergence, or the Hessian at the PySCF one does not carry the archived branches' start frequency."

  - id: lesson.a_summary_nothing_rederives_is_not_evidence
    kind: lesson
    source: {path: ".agents/charter/pyscf.md", anchor: "Result contract v9 adds ``ts``"}
    note: "Six adversarial mutations of a real archived v8 IRC artifact -- the start spectrum made all-real, the mass table set to 1.0, two interior frames swapped, the energies reversed, the path appended to itself walked back to the saddle, the start Hessian replaced -- all validated, because every derived scalar was read and none re-derived. The class: a summary written beside the arrays it came from, with nothing re-deriving it, is a claim the bytes need not support."
    falsifier: "A derived scalar an artifact records beside its primitives survives a mutation of those primitives without a finding."

  - id: lesson.an_order_is_a_property_of_a_stationary_point
    kind: lesson
    source: {path: ".agents/charter/validity-rules.md", anchor: "The mode count is not stationarity, and one organ read it as such."}
    note: "A count of imaginary modes is not an order. The archived water_stretched_hess -- three real modes at max|g| = 0.0185 Eh/Bohr, 41x geomeTRIC's criterion, on the artifact the host's own sensor flags -- was certified 'a minimum'. An order is a claim about a geometry, on a surface, from a Hessian taken there, at a gradient that says the point is stationary at all; the receipt names all four and the gradient refuses."
    falsifier: "A host-rendered stationary-point order is issued for a structure whose measured gradient exceeds the optimiser's criterion."

  - id: lesson.the_authoring_interpreter_is_not_the_controllers
    kind: lesson
    source: {path: ".agents/charter/pyscf.md", anchor: "An earlier submission of that same batch (2141121)"}
    note: "Ten real engine runs produced no receipt at all: a MappingProxyType dataclass default that Python 3.12 accepts, 3.11 refuses, and the deployed controller is 3.11. Every test on the authoring interpreter was green. A probe that imports every module under the compute interpreter now runs beside the suite."
    falsifier: "Importing every chemsmart module under the deployed controller's and compute interpreters raises nothing the suite did not catch."

  - id: negative.a_linear_polyatomic_hessian_was_refused
    kind: negative_result
    source: {path: ".agents/charter/pyscf.md", anchor: "that Hessian is the first on a linear polyatomic in this corpus"}
    note: "The mode-count rule expected 3N-6 unless its own geometric linearity test said otherwise, and that test answers about 0.01 degrees for a triatomic. HNC reached by a live PySCF IRC is 0.047 degrees from linear: PySCF's correct 3N-5 spectrum was refused with four findings and typed failed_native, and the independent reconstruction at the same rank dropped half a degenerate bending pair. It is the node a live goal's analysis chain was waiting on. Repaired by taking the translation-rotation rank from the run's own mode count."
    falsifier: "A Hessian on a molecule near the linear boundary is refused, or validated, on a geometric tolerance rather than on whether its spectrum reconstructs at the rank its own count implies."

  - id: lesson.a_promise_belongs_to_the_structure_not_the_node
    kind: lesson
    source: {path: ".agents/charter/pyscf.md", anchor: "a Hessian that confirms a saddle was being typed"}
    note: "ORCA runs OptTS Freq as one node; PySCF and xTB split the search and the Hessian. Judged by its own job type, the Hessian half typed two live saddles failed_wrong_stationary_point for carrying the one imaginary mode they were run to find. The promise is about the structure, and the node that produced it already said what it was searching for, so a fixed-geometry curvature node inherits it -- which also catches the reverse, a saddle search that converged onto a minimum, which its own artifact cannot detect."
    falsifier: "The same physics gets a different terminal word because one program runs the search and the Hessian as one node and another runs them as two."

  - id: result.an_irc_endpoint_reached_by_minimising
    kind: result
    source: {path: ".agents/charter/pyscf.md", anchor: "its backward branch recording 77 frames"}
    note: "The H2CO -> H2 + CO backward branch records 77 frames of which 37 are Gonzalez-Schlegel path steps and 40 are the minimisation after them; its endpoint is separated H2 (0.730 A) and CO (1.1135 A). The first recorded case of reached_by: minimisation_from_path_tail, and the case the record exists for -- a previous round delivered an H2...CO complex as 'separated H2 + CO'."
    falsifier: "A branch whose tail geomeTRIC minimised reports the same account as one that stepped all the way, or the frame split does not sum to the frames recorded."

  - id: negative.small_molecules_disable_the_basin_sensor
    kind: negative_result
    source: {path: ".agents/charter/pyscf.md", anchor: "the basin sensor's three-heavy-atom floor stopped"}
    note: "Every node of all four campaign goals raised geometry.same_structure_comparison_not_made: the heavy-atom RMSD floor is three and the molecules had two. The sensor said it stopped, which is correct; the campaign's own molecule choice removed the basin and same-structure evidence from the whole round."
    falsifier: "A campaign of two-heavy-atom molecules obtains basin or same-structure evidence, or the floor is shown to be unnecessary below three heavy atoms."

  - id: result.capability_scale_stopped_driving_context_scale
    kind: result
    source: {path: MAINTENANCE.md, anchor: "byte-identical* with 2,000 synthetic entries added"}
    note: "The stem was 19 tools / 92,848 B and every guide open 30 / 120,561 B, both under ceilings that had been raised six times. A catalogue plus host-side search makes the initial context 8 tools / 45,150 B and independent of how many entries exist."
    falsifier: "Adding entries to the catalogue changes what a first request carries, or the initial context is shown to be larger than the stem it replaced on any real session."

  - id: lesson.a_search_that_returns_a_name_costs_a_turn
    kind: lesson
    source: {path: MAINTENANCE.md, anchor: "A search that returns a name costs a turn for nothing."}
    note: "Live A (2026-09-20) planned correctly in 25 searches, four of them for one exact name it already had; the repaired A2 took 5 searches, 9 provider turns against 15, 15 tool calls against 46, and reached the same plan. Finding and loading are one turn, which is also what the reference backend does."
    falsifier: "A backend that returns bare names shows the same search count as one that loads, on matched requests."

  - id: result.an_operation_the_stem_hid_was_used_by_name
    kind: result
    source: {path: ".agents/charter/architecture.md", anchor: "Loading an act loads its family's reference"}
    note: "center_of_mass was one of fifteen operations unnameable from the stem; naming it was refused with 'is not one of [29 names]', and a recorded session rebuilt it from fifteen arithmetic nodes with integer masses. Two live sessions on the catalogue found about_operations_geometry and planned the named operation."
    falsifier: "A session that needs an operation the catalogue holds rebuilds it from arithmetic, or cannot retrieve the entry that owns it."

  - id: negative.deferring_the_planning_tool_was_measured_and_refused
    kind: negative_result
    source: {path: MAINTENANCE.md, anchor: "The initial context fell"}
    note: "plan_scientific_workflow is 27,531 B, 61% of the initial context. Deferred, the first request falls to 17,228 B -- but it retrieves at rank 4 of 5 for 'plan an optimisation then a frequency calculation of the optimised structure' and rank 2 for 'set up a workflow of calculations'. It is the act the whole approval chain hangs on, so the bytes were not taken."
    falsifier: "A search probe retrieves plan_scientific_workflow at rank 1 for the ordinary phrasings, or a session that fails to retrieve it is shown to lose nothing."

  - id: result.search_first_beat_its_own_control_arm
    kind: result
    source: {path: MAINTENANCE.md, anchor: "The three-way arm that settled it."}
    note: "One request, three arms, same tree, same provider profile, same plan at the end: eager (control) 12 turns / 169,896 B initial context; host_search 9 turns / 45,150 B. The smaller-context arm took fewer turns, not more. N=1 request; the mechanism the reference documentation offers is that selection accuracy falls above ~30-50 visible tools."
    falsifier: "On matched requests, the eager arm completes in fewer or equal turns than host_search, or the difference does not survive a second request."

  - id: negative.a_zero_result_search_was_read_as_an_absence
    kind: negative_result
    source: {path: MAINTENANCE.md, anchor: "A search that returns a name costs a turn for nothing."}
    note: "Live D issued three searches that returned no new matches; all three were answered correctly, with the tool it wanted listed under already_available. It searched again anyway. Zero matches beside a populated already_available and zero beside an empty one are different answers and the reply gave them one sentence."
    falsifier: "A session meets a zero-match search whose already_available names what it needs and uses it without searching again."

  - id: result.a_decomposed_planner_keeps_its_digests
    kind: result
    source: {path: ".agents/charter/architecture.md", anchor: "a workflow drafted stage by stage"}
    note: "The aggregate planner became one constructor per scientific act plus a finaliser over a host-owned draft. The finaliser assembles the argument set the aggregate tool took and calls the same handler, so plan_sha256, draft_sha256 and the V2 plan digest are byte-identical whichever surface authored the nodes -- which is what resume and plan_reproduction_rule key on."
    falsifier: "The same nodes drafted stage by stage and sent in one payload produce a different canonical digest, or a global check stops firing at the finaliser."

  - id: result.one_analysis_kind_loads_only_its_own_schema
    kind: result
    source: {path: MAINTENANCE.md, anchor: "a thermochemistry task ended at 89 KB"}
    note: "Live L2 (2026-09-21, planning-only): a Gibbs-free-energy task loaded plan_thermochemistry, plan_calculation_stages, plan_result_extraction and plan_claim_rendering and never loaded plan_quantity_expression, plan_scientific_validation or plan_unsupported_external -- 28,368 B of schema that the aggregate payload made every session read."
    falsifier: "A task needing one analysis kind loads the schema of a kind it never plans, or the initial context stops being independent of the number of constructors."

  - id: negative.decomposition_costs_turns_and_repeats_prose
    kind: negative_result
    source: {path: MAINTENANCE.md, anchor: "Decomposition buys per-task exposure and costs turns"}
    note: "Ten tools total 68,579 B against the aggregate's 27,920, because six analysis constructors repeat ~5.1 KB of shared inputs/outputs/dependencies prose; a session needing every kind loads more than before. On one identical request across both legs, turns rose 9 -> 11 and searches 5 -> 12 while end-of-session schema fell 159 -> 134 KB."
    falsifier: "Moving the shared field guidance into the analysis family reference removes the repetition without losing an earned sentence, or a matched-request comparison shows no turn cost."

  - id: result.what_a_session_sees_is_mostly_on_its_own_task
    kind: result
    source: {path: MAINTENANCE.md, anchor: "Need was 100% on every session of both legs"}
    note: "Measured by .agents/research/loop/context.py over ten recorded sessions: every capability a session called was in view before the call, and the share of loaded entries on a family the session's own acts touched rose from 0.44-0.71 (first leg) to 0.76-0.92 (second). The remainder is references matching one word of a query."
    falsifier: "A session calls a capability that entered view only after the call, or the on-family share falls below the first leg's range on matched requests."

  - id: negative.a_loaded_reference_cannot_be_told_from_a_read_one
    kind: negative_result
    source: {path: MAINTENANCE.md, anchor: "A reference entry's body *is* its description"}
    note: "Loading is the only event a reference has, so relevance is exact for acts (which are called) and an upper bound for knowledge. Nothing in this round changes that, and redesigning references to emit a read event would put a turn between a model and text it already has."
    falsifier: "A measure distinguishes a reference the model used from one that merely arrived, without costing a turn or a second copy of the text."

  - id: lesson.a_rendering_instruction_is_not_a_permission_gate
    kind: lesson
    source: {path: ".agents/charter/architecture.md", anchor: "byte-identical every turn"}
    note: "Marking wire deferral from 'not callable yet' made the tools array change on every discovery, invalidating the provider's whole cache -- the thing deferral exists to protect. Callable-now grows; deferred-on-the-wire is fixed when the session starts. Eleven witnesses asserted the marking matched the growing set, so they pinned the defect."
    falsifier: "A session's tools array or cache_control position differs between two turns that discovered something in between."

  - id: result.gaussian_ran_through_chemsmart_on_cuhk_20260921
    kind: result
    source: {path: ".agents/charter/other-programs-probe-providers.md", anchor: "Gaussian 16 C.02 has been driven through ChemSmart on a Slurm target"}
    note: "Gaussian had never been driven through ChemSmart on this target. Fifteen small jobs over two Slurm allocations (2142374, 2142393) covered every declared job type on the R8 profile's GAUSSIAN block. Physics that holds across them: a distorted H2O2 relaxed to O-O 1.4557 A (experiment 1.452) with six real modes; the HCN/HNC saddle carried one imaginary mode at -1146.1 cm^-1 and its forward IRC branch changed connectivity to N-H 1.089 A while the reverse changed none and ended at C-H 1.106 A; the H2O2 cis saddle carried one imaginary mode at -635.3 cm^-1 and its two branches fell symmetrically to +/-40.1 degrees with no connectivity change; the 13-point torsion scan is even in the dihedral to 2e-7 Eh and gives a cis barrier of 8.87 kcal/mol; a constrained optimisation reached its frozen O-O to 1.550000 A."
    falsifier: "A ChemSmart-driven Gaussian job of a declared job type on this target produces a log the reader cannot classify, or one whose reported quantities disagree with the log's own printed values."

  - id: negative.a_relaxed_scan_classified_itself_as_a_single_point
    kind: negative_result
    source: {path: ".agents/charter/other-programs-probe-providers.md", anchor: "written both for a relaxed scan and for a constrained optimisation, and a"}
    note: "A job type is a gate on every selector, so a misclassified result is an unreadable one. Gaussian's route-word chain matched the literal `opt=modred`, so `opt=(modredundant,maxstep=10)` fell past the `opt` branch (which excludes any route naming modred) and out of the chain as `sp` -- the spelling an archived real relaxed scan in this repository uses. The plainly spelled scan fared no better: it classified as `modred`, for which the reader declared nothing. Neither failure was visible as an error; both appeared as a completed result that answered nothing, or answered under another job's semantics."
    falsifier: "A completed Gaussian log whose ModRedundant section drives a coordinate is classified as anything but a scan, or one whose rows only freeze coordinates as anything but a constrained optimisation."

  - id: lesson.a_classification_written_into_a_setter_nothing_reads_was_never_made
    kind: lesson
    source: {path: "chemsmart/io/gaussian/output.py", anchor: "assignment reaches ``GaussianRoute.jobtype``'s setter, which"}
    note: "The scan/constrained-optimisation distinction had been computed for years: the Gaussian mixin assigns `self.jobtype = \"scan\"` while parsing the ModRedundant section. The assignment reaches a property setter that stores `_jobtype`, and that property's getter recomputes from the route and never reads the field. So the classification was not late or order-dependent -- it did not exist. This is the CONDUCT pattern `a value the host resolves and then discards leaves a boolean nothing can audit`, in a form where even the discarded value looked stored."
    falsifier: "A host value assigned through a property setter is read back by any consumer of that property without the getter being changed."

  - id: negative.a_duplicated_route_keyword_dropped_the_hessian_silently
    kind: negative_result
    source: {path: ".agents/charter/other-programs-probe-providers.md", anchor: "answers a route naming ``freq`` twice by running no frequency step and"}
    note: "Probing whether the reader survives `freq=HPModes` was void, for a reason worth more than the probe. The route parameter is appended verbatim beside the project section's own `freq`, so Gaussian 16 C.02 received `# opt freq b3lyp 6-31G* freq=hpmodes`, ran an FOpt with no frequency step, and terminated normally (job 2142393). The host behaves correctly -- no frequencies is reported as no frequencies -- but a run asked for a Hessian returned none and neither program said so. The standing charter objection to Gaussian normal modes is therefore still untested here."
    falsifier: "A Gaussian route carrying a keyword twice runs the calculation that keyword requests, or ChemSmart refuses to write such a route."

  - id: negative.a_green_gaussian_review_was_never_decided
    kind: negative_result
    commit: "0fee0ff4"
    note: "Two goals on two chemically different molecules (Slurm 2142394 H2O2, 2142397 CH2O) took a Gaussian opt-feeds-sp workflow through project promotion, validation, compilation, safe preview, program validation, preflight and the execution review with zero findings, and both settled `execution_wave_decision_pending`. `select_execution_wave` was in neither session's callable set (29 and 35 capabilities loaded) and the catalogue search for the word `execute` returns `inspect_workflow_draft` and `about_pyscf`, not it; `run the calculation` does return it. So the block is retrieval in a shared layer, not the Gaussian path, and no Gaussian engine call has been made under the approval chain."
    falsifier: "A session that has a ready node and a standing approval reaches an engine call without searching a phrase other than the verb it would naturally use, or `select_execution_wave` ranks for `execute`."

  - id: lesson.two_organs_answer_the_reached_structure_and_one_asks_the_reader
    kind: lesson
    source: {path: "chemsmart/agent/execution.py", anchor: "lift, so this edge and ``build_reached_geometry`` answer one question"}
    note: "A round's premise held that a reader declaring no structural state makes every geometry route refuse that program. Half true, and the half that is false is the interesting one. `build_reached_geometry` -- the explicit lift a session calls -- asks the reader for a selector in the `as_reached` state, so it did refuse every Gaussian result. The in-approval producer edge for opt and ts does not: `handoff_optimized_native_geometry` opens the log itself, applies its own job-type gate and its own convergence test, and reads `optimized_structure`. Two organs answer `the structure this run reached` and only one of them asks the reader that owns the question."
    falsifier: "The executor's optimized-geometry handoff and the reader's `reached_positions` disagree about which structure a completed result reached, for any program."

  - id: result.gaussian_executed_under_the_approval_chain_20260921
    kind: result
    source: {path: ".agents/charter/other-programs-probe-providers.md", anchor: "the Agent executed ``opt`` and ``sp``"}
    note: "The first approved Gaussian executions. Once `select_execution_wave` was surfaced from typed state and its description said that it runs the calculations, the same two goals -- re-issued unchanged, task text byte-identical -- found it, called it, and ran. g3 (H2O2, Slurm 2142407) and g4 (CH2O, 2142428) each relaxed a deliberately distorted geometry and took a single point on the structure the optimisation reached, each over two cycles with one revision admitted, and each settled `achieved` under the host completion gate. The opt of each reproduces the same calculation run through the human CLI to every printed digit: H2O2 identical reached geometry to 0.0 A and identical six frequencies, CH2O the same. Each single point ran on the reached structure and not the supplied one -- CH2O C=O 1.206270 A and H-C-H 115.220 degrees in both, energy agreeing to 1e-9 Eh. The measured order matters: with the flags false the same goal settles `returned_to_human` in twelve seconds with `bounded execution has no executable jobs`, before any planning, so the declaration cannot follow the run it admits."
    falsifier: "An approved Gaussian opt or sp on this configuration fails to execute, or its result disagrees with the same calculation through the human CLI by more than optimiser convergence."
  - id: result.pyscf_decomposition_cli_20260921
    kind: result
    source: {path: ".agents/charter/pyscf.md", anchor: "Result contract v10 records what a total energy is made of."}
    note: "PySCF computes the continuum's polarisation energy and SMD's CDS term, adds both into e_tot and keeps them in scf_summary; the driver dropped them, so every solvated PySCF energy in this corpus was an opaque total. Five runs through the ordinary CLI (CUHK 2142387, water at B3LYP(G)/def2-SVP, every receipt validated): SMD polarises by -10.122 kcal/mol with a CDS term of +1.447, C-PCM at the same geometry and the same level by -6.491 with no CDS term at all, and D3(BJ) contributes -0.360 with the corrected total below the uncorrected one by exactly that. PySCF's own recomputation from each applied spec reproduces every stored term to 3e-16 Eh, and to 1.4e-7 Eh on the optimisation, which is an SCF re-convergence difference. The names and units are the ones the ORCA reader already served, so the two programs' solvation terms are comparable rather than merely both present."
    falsifier: "A solvated PySCF result under contract v10 reads, through the selector plane, the same as a gas-phase one; or a stored term differs from PySCF's own scf_summary at the artifact's geometry by more than an SCF re-convergence."

  - id: negative.a_predicate_read_as_a_value_validates_nothing
    kind: negative_result
    source: {path: ".agents/charter/pyscf.md", anchor: "One defect the round's own probe found is repaired where it was made."}
    note: "_validate_correlated_results -- 'the components are finite and sum to the total the artifact states' -- reported nothing on any artifact since it was written. _finite_number is a predicate and was read as a value: every entry of a results mapping read back from HDF5 is a NumPy array, so each component answered False, the sum compared False + False with False and agreed, and a missing component was never reported because a bool is never None. Removing a whole component from a real CCSD(T) artifact produced no finding. The class is mechanically detectable: a name that answers a value question with a bool."
    falsifier: "A validator reports no finding when a quantity it names is removed from the artifact it is given."

  - id: lesson.an_absence_names_which_absence_it_is
    kind: lesson
    source: {path: ".agents/charter/pyscf.md", anchor: "the three absences are different facts that each name themselves"}
    note: "Three different facts reached a session as one sentence, 'dataset absent': a run that attached no continuum, a PCM-family model that has no cavitation term by construction, and an artifact written before the contract that records either. The unit audit answers whether a dataset is there and stored as we read it; it cannot answer why this selector is not, and it ran first, so the accessor's own reason never left the reader. The repair is general -- when the audit finds nothing the accessor is asked what the absence means, and the generic sentence stands wherever it has nothing to add."
    falsifier: "A session that meets a refused quantity cannot tell from the refusal alone whether to change the settings, change the model, or read a newer artifact."
  - id: commit.88a6051d
    kind: commit
    commit: "88a6051d"
    note: xTB - the solvation model, the solvent and the five ALPB/GBSA free-energy terms, declared on the reader beside the accessors that read them.
  - id: negative.every_archived_xtb_run_has_solvation_off
    kind: negative_result
    source: {path: .agents/charter/solvation-and-populations.md, anchor: "every archived xTB run has solvation switched off"}
    note: "False when written, and it withheld a whole surface. tests/data/XTBTests/outputs/p_benzyne_sp_alpb_toluene and p_benzyne_opt_alpb_toluene are GFN2-xTB/ALPB(toluene) runs whose SUMMARY blocks print Gsolv, Gelec, Gsasa, Ghb and Gshift; they arrived in 855dc848 with the xTB parser the sentence was written beside, and nothing had read them. The gate that carried the sentence now asks each declaring reader to name the archived solvated result its meaning was read from (e81b8f1e), and the xTB declaration follows in 88a6051d."
    falsifier: "No archived xTB result on this tree resolves a solvation_model other than gas_phase."
  - id: result.xtb_solvation_terms_20260921
    kind: result
    commit: "88a6051d"
    note: "Seven GFN2-xTB runs through the public CLI on CUHK (Slurm 2142381, 2142385) read back through extract_result_quantities. Gsolv equals the sum of its four parts to 5e-18 Eh on every solvated result. ALPB(water) stabilises water by -11.389 kcal/mol and destabilises methane by +0.158, the hydrophilic/hydrophobic sign contrast; the water total is dominated by Ghb (-9.343) and not by Gelec (-3.883), which is the opposite of the expectation basis the live Agent session declared for it. GBSA(water) on the same solute gives -7.044 kcal/mol, so the model name must travel with the number. E(solvated) - E(gas) at the same geometry is -8.753 kcal/mol for water against a reported Gsolv of -11.389, the +2.636 difference being the solute's polarisation cost, and +0.180 against +0.158 for methane, which barely polarises."
    falsifier: "A solvated xTB result's Gsolv differs from the sum of its printed parts by more than print rounding, or ALPB and GBSA agree on one solute in one solvent, or the aqueous continuum stabilises methane."
  - id: result.xtb_gfnff_solvation_is_undecomposed
    kind: result
    commit: "7f3f2c85"
    note: "A GFN-FF/ALPB(water) single point (CUHK 2142381) reports its model, its solvent, Gsasa, Ghb and Gshift, and prints no Gelec at all -- a force field has no self-consistent-charge electrostatics to name. Its Gsolv (-0.024626451737 Eh) is therefore NOT the sum of the parts it prints (-0.017924440181 Eh); 0.0067 Eh of it is undecomposed. Read back on the repaired tree (2142395) the electrostatic term is reported typed-absent with that reason, the other four are served, and the closure identity abstains because it is only checked when all five are present. Checking it unconditionally would have refused a correct result."
    falsifier: "A GFN-FF solvated xTB result prints a Gelec line, or its Gsolv equals the sum of the terms it does print."
  - id: commit.f7e39390
    kind: commit
    commit: "f7e39390"
    note: xTB - the printed normal modes attached to the structure they describe, with the frame check the split between g98.out and the geometry files makes necessary.
  - id: lesson.a_helper_written_and_never_called_is_a_dead_route
    kind: lesson
    source: {path: CONDUCT.md, anchor: "the mechanism is right where it is computed and unconnected where it is consumed"}
    note: "XTBG98File._attach_vib_metadata existed in full and was called from nowhere, while the ORCA and Gaussian readers each call their own from all_structures. Every selector reading modes off the OUTPUT worked, so nothing looked broken; the program-neutral displace_along_vibrational_mode reads output.molecule.vibrational_modes and refused every xTB result with 'this result prints no normal modes' -- on an archived Hessian carrying a -3253.79 cm-1 mode. The route out of a saddle that the repair menu and the terminal-state rules both name was dead for one program, and the thing that would have connected it had already been written."
    falsifier: "A program's reader exposes a quantity on the output and on its molecule by two paths that a single consumer can tell apart."
  - id: result.xtb_saddle_escape_20260921
    kind: result
    commit: "f7e39390"
    note: "The chain run end to end on CUHK (Slurm 2142401, read back 2142402), GFN2-xTB, five engine calls. Exactly planar D3h ammonia optimises to a stationary point (gradient norm 3.58e-07 Eh/a0) whose Hessian carries one imaginary mode at -971.31 cm-1 with the three hydrogens leading -- the umbrella coordinate. Stepping 0.30 A along it in each direction and relaxing gives two structures equal in energy to every printed digit, as two mirror images must be, whose own Hessian has no imaginary mode: 1160.78, 1593.31, 1593.31, 3425.0, 3476.94, 3476.99 cm-1, with the C3v degenerate pairs where C3v puts them. Inversion barrier +6.110 kcal/mol. A live Agent goal then walked the same route itself (2142404): it read the saddle, displaced in both directions through the host and selected the wave that relaxes them."
    falsifier: "A displacement along a saddle's imaginary mode relaxes to a structure that still carries one, or the two directions along one umbrella mode reach minima of different energy."
  - id: negative.two_organs_hand_a_reader_two_types_for_one_path
    kind: negative_result
    commit: "001dab68"
    note: "displace_trusted_geometry_along_mode passes a Path and build_stationary_point_characterisation passes str(artifact.path). Every reader but xTB's only wraps the value in its own parser and so tolerates both; the xTB reader navigates to the calculation directory and met the str at .parent, so a live session asking what its stationary point was got AttributeError: 'str' object has no attribute 'parent', untyped, twice in one session (2142404). Normalised in the reader because that is where the navigation is; the disagreement between the two organs is not xTB's and is left for the layer that owns both."
    falsifier: "Both organs hand every reader the same type, or a reader that navigates the path is given one it cannot navigate and still answers."
  - id: negative.an_xtb_hessian_matrix_has_no_consumer
    kind: negative_result
    commit: "855dc848"
    note: "XTBHessianFile.hessian is parsed and reaches nothing, and this round did not change that. There is no hessian selector in the shared vocabulary at all -- ORCA's Hessian travels as a FILE over a producer edge, never as a quantity -- and a producer edge needs a live CLI option of the CONSUMING program to receive it: both HESSIAN_CONSUMER_ROLES entries name producer_program orca and consumer_program orca, and the xTB Click group offers no option that reads a Hessian in. A selector would carry a 3Nx3N matrix into an arithmetic that has no operation for it. The honest state is that the matrix is parsed and unreachable, for a structural reason rather than an oversight."
    falsifier: "A ChemSmart CLI option of any program accepts an externally supplied Turbomole-format Hessian, or a HESSIAN_CONSUMER_ROLES entry names a producer other than orca."
  - id: result.orca_irc_path_and_endpoints_20260921
    kind: result
    source: {path: .agents/charter/orca-scan-irc-modred.md, anchor: "ORCA prints it as the IRC"}
    note: "ORCA 6.1.1 on CUHK Charles, HCN -> HNC at B3LYP/def2-SVP (jobs 2142375, 2142379, 2142403). Each single-direction PATH SUMMARY opens on the saddle; the direction-both table opens 45.5 kcal/mol below it. The forward branch descends 33.40 kcal/mol to an HNC endpoint, the backward one 45.47 kcal/mol to HCN, and optimising those two endpoints gives two true minima (lowest real modes 549 and 763 cm^-1) 13.66 kcal/mol apart with a 47.77 kcal/mol barrier from HCN. Both branches stopped 0.71 and 2.31 kcal/mol short of their own minima, which is what irc_converged = 0 said."
    falsifier: "An ORCA IRC branch whose path table's first row is not the structure the log printed, or whose endpoint sidecar does not carry the table's last energy."
  - id: negative.i_read_the_wrong_hessian_block_by_eye
    kind: negative_result
    source: {path: chemsmart/io/orca/output.py, anchor: "A forward scan that returns its"}
    note: "Reading an OptTS log with grep, I reported -1133.34 cm^-1 as the converged saddle's imaginary mode and put it in a commit message. It is the initial Hessian's, at the guess geometry; ORCA prints two blocks and the converged one is -1121.71 cm^-1, which is what ORCAOutput returns and what the live goal delivered. The reader had this right before I did, by the rule its own docstring states."
    falsifier: "An ORCA OptTS log whose first printed vibrational block is the one at the converged geometry."
  - id: negative.an_engine_written_sidecar_is_not_a_registered_geometry
    kind: negative_result
    source: {path: chemsmart/agent/execution.py, anchor: "what an engine wrote is evidence the"}
    note: "The ORCA charter topic said an IRC's trajectory sidecar 'enters the typed layer as a registered geometry artifact and is readable there today'. It does not: the workspace XYZ scan bars every node branch root, so no file an engine wrote is ever registered as a coordinate artifact. The path and the endpoint had to be read by the program's own reader instead."
    falsifier: "A workspace scan registers as a coordinate artifact a geometry file an engine wrote into a node branch."
  - id: lesson.a_path_string_is_not_a_file_identity
    kind: lesson
    source: {path: chemsmart/jobs/orca/writer.py, anchor: "String equality is not file identity."}
    note: "Three ORCA staging guards skipped a copy when two absolute paths compared equal as strings. /project/<group> is a symlink to /lustre/project/<group>, so one file had two absolute spellings, shutil.copy2 raised SameFileError, and three IRC branches died in the writer before ORCA was reached (job 2142378). Every existing writer test ran under one spelling of tmp_path."
    falsifier: "A staging or idempotence guard decides file identity by comparing path strings and still behaves correctly when a parent directory is reached through a symlink."
  - id: negative.a_gaussian_irc_node_cannot_say_which_way_it_walks
    kind: negative_result
    source: {path: ".agents/charter/other-programs-probe-providers.md", anchor: "What a plan still cannot say is a direction per *node*"}
    note: "The architecture is one direction per node -- ORCA's irc and PySCF's both are -- and `run gaussian irc` has always taken `--direction`, but nothing the Agent writes could say it. Measured live on CUHK 2026-09-21: goal g1 wrote `irc: {direction: forward}` and `irc: {direction: reverse}` as two projects for two nodes and the loader answered `invalid` to both, so the session fell back to one directionless node that writes both branches; earlier in the same goal `irc: {maxpoints: 50}` was refused the same way and became `additional_route_parameters: maxpoints=50`, a bare token beside the keyword it belongs inside. The loader half is fixed: g2 validated `irc: {maxpoints: 60, stepsize: 0.1}` an hour later on the same target, and g3 then executed `irc-forward` and `irc-reverse` as two nodes of one direction each. What remains is narrower than the sentence first said. The direction reaches the route through the project a node names, so a plan CAN express one direction per node; what it cannot do is carry the direction in the node itself -- CommandNodeIntentV1 holds charge, multiplicity and internal_coordinates and nothing else a job owns -- so the two plan-time gates, which are asked only for a program and a stage, still treat every Gaussian irc node as two-result and refuse it conservatively."
    falsifier: "A planning node itself states one IRC direction without a per-node project, or a geometry edge leaves a one-direction Gaussian IRC node inside the same approval."
  - id: result.gaussian_ts_and_irc_executed_under_the_approval_chain_20260921
    kind: result
    source: {path: ".agents/charter/other-programs-probe-providers.md", anchor: "``ts`` and ``irc`` were earned together"}
    note: "The first Gaussian saddle search and reaction path the Agent has run under the approval chain (CUHK Slurm 2142871, B3LYP/6-31G(d), two cycles, one revision admitted, settled achieved_with_observations). Handed a malonaldehyde enol with the enolic hydrogen put at the O...O midpoint, the search reached the C2v proton-transfer saddle in 29.0 s: one imaginary mode of twenty-one at -1231.2699 cm^-1, O...O 2.3781 A against the input's 2.683, and the C-O and C-C pairs equal to 2e-4 A although the input's were not, so the symmetry is the engine's answer. The host's geometry handoff carried that structure into the path, which the verifier read as the two one-direction results it is: 15 points per branch, net reaction coordinates 1.50918 and 1.50917, endpoints -267.148804378 and -267.148803942 Eh, mirror images to 4.4e-7 Eh across a 3.447 kcal/mol barrier that nothing told the engine to expect. Each branch reads with its own direction, 17 frames and trajectory_connectivity_changed = 1. The two logs are archived as tests/data/GaussianTests/outputs/malonaldehyde_pt_ircf.log and malonaldehyde_pt_ircr.log, the first Gaussian IRC pair this repository has held. Repeated on twenty-one heavy atoms (g3, Slurm 2142917, 1,3-bis(4-cyanophenyl)propane-1,3-dione, 42 minutes wall, settled achieved_with_observations): the saddle carried one imaginary mode of eighty-seven at -1106.1223 cm^-1 with O...O 2.3636 A and its C-O pair identical to four decimals, and the path ran as two nodes of one direction each -- the shape g1 and g2 had been refused -- each qualified by the host separately, 20 points apiece to the same net reaction coordinate 2.05099, ends mirror-imaged to 1.9e-7 Eh across a 2.16 kcal/mol barrier, 23 frames and trajectory_connectivity_changed = 1 on both. The lower barrier at the shorter O...O across the two molecules is the resonance-assisted hydrogen bond, measured rather than asserted."
    falsifier: "An approved Gaussian ts or irc on this configuration fails to execute, or a reached saddle's spectrum or a branch's endpoint disagrees with the same calculation through the human CLI beyond optimiser convergence."
  - id: lesson.a_refusal_names_an_act_not_an_eventual_state
    kind: lesson
    source: {path: CONDUCT.md, anchor: "A refusal lives in code only when it protects an invariant"}
    note: "A gate that refuses correctly and then describes the world afterwards is a gate the session pays for twice. The plan-time refusal for a stage that writes several results said to read each one 'as a registered result after the run'; nothing is registered while a plan is being built, so the route it named did not exist in that turn. Two live sessions (CUHK g1 and g2, 2026-09-21) each planned the same shape twice before withdrawing the extraction and planning the walk alone, which is the act the sentence never named. It names the act and the node now; that wording was written after both runs and no run has met it."
    falsifier: "A session meets this refusal and repeats the same plan shape before recovering."

  - id: negative.a_recorded_response_dielectric_had_no_reader
    kind: negative_result
    source: {path: .agents/charter/pyscf.md, anchor: "a toluene fixture shows the divergence instead of hiding it behind water"}
    note: "The recording was true and the reading never existed. status/stages/td/solvent has carried static_eps_applied and response_eps_applied since contract v5, tests/test_pyscf_archived_results.py::test_the_solvated_response_records_the_dielectric_it_applied has asserted both on the archived water_td_cpcm_toluene for a round, and no selector, level field or receipt served either -- so what reached a session was the pair (cpcm, toluene) and nothing about the 1.78 the excitations ran on. Two such spectra differenced and delivered as a solvatochromic shift would pass every digest, unit, geometry and surface check. Same class as the predicate read as a value and the classification written into a setter nothing reads: right where computed, unconnected where consumed. Closed by 583cb8f0 at the reader, with no contract bump, because the bytes were already there."
    falsifier: "A PySCF result reader on this tree answers neither solvent_dielectric nor excitation_response_dielectric, or a solvated td artifact's status/stages/td/solvent is absent."
  - id: result.pyscf_response_permittivity_is_waters
    kind: result
    commit: "583cb8f0"
    note: "Read through the shared extraction plane off the archived water_td_cpcm_toluene (real PySCF 2.14.0 through the ordinary CLI, with PySCF's own reference.json beside it): the density was polarised with toluene's 2.3741 and the excitations were answered with 1.78, which reference.json independently recomputes as td_response_eps. Checked against PySCF's own solvent_db rather than a recalled constant: water's n^2 is 1.7764, so the applied number is water's to 5e-3, and toluene's own n^2 is 2.2383, 0.46 away. PySCF 2.14 hard-codes the non-equilibrium response permittivity in solvent/_attach_solvent.py, so the entire solvent dependence a PySCF spectrum can carry enters through the ground-state density, whatever solvent the run is named for. No Agent goal has run a solvated spectrum, so the two selector cells are declared and tested and not qualified."
    falsifier: "A solvated PySCF td artifact on this build records a response_eps_applied that differs between two solvents, or that is not within 5e-3 of solvent_db['water'][0]**2."
  - id: result.pyscf_dans_solvatochromism_20260922
    kind: result
    commit: "304de2af"
    note: "First live Agent goal on a solvated PySCF spectrum (CUHK Slurm 2142833, settled achieved, delegated approval claude-researcher-pyscf-owner-delegated). DANS, 20 heavy atoms, one CAM-B3LYP/def2-SVP geometry a PySCF opt reached, carried into three td nodes through the optimised-geometry edge -- each node's supplied geometry equals the reached structure to 4.9e-11 A, so only the continuum differs. Bright root 1: gas 3.51179 eV / 353.05 nm / f 1.24279; C-PCM n-hexane 3.31881 / 373.58 / 1.35775; C-PCM acetonitrile 3.20362 / 387.01 / 1.31496; the host's arithmetic gave the shift as -0.11519 eV and +13.43 nm. The static permittivities the two solvated runs applied differ by a factor of 19 (1.8819 and 35.688) while the permittivity their responses applied is the same 1.78 in both, so that whole computed shift is ground-state density polarisation and the excited-state fast response contributed identically in both solvents. The session chose CAM-B3LYP itself, recording B3LYP and PBE0 as rejected for placing the CT state too low."
    falsifier: "Two PySCF td runs on one geometry at one level in different solvents record different response_eps_applied, or a solvated td node's supplied geometry differs from its producer's reached geometry by more than the handoff tolerance."
  - id: negative.the_shift_was_delivered_without_the_permittivity_that_bounds_it
    kind: negative_result
    commit: "304de2af"
    note: "Making a fact reachable is not the same as a session using it. In cycle 1 this goal's own plan requested solvent_dielectric and excitation_response_dielectric on both solvated nodes and on neither the gas node -- from a task that names no selector, permittivity or solvent model -- which is the discovery half working. Woken after the optimisation, the session replanned and bound only excitation_energies and oscillator_strengths, and delivered -0.11519 eV as the n-hexane-to-acetonitrile shift without saying that the response ran at 1.78 in both solvents. The numbers were still on every receipt it held: the level record inside each extraction receipt's digest carried both permittivities on the solvated nodes and neither on the gas one. So the two selector cells stay tested rather than qualified, and the open question is whether a level field a session does not have to ask for is read at all."
    falsifier: "A live session that extracts a solvated PySCF spectrum binds excitation_response_dielectric, or states the applied response permittivity in a claim about a solvent shift."
  - id: result.xtb_cheap_hessian_is_not_a_minimum_20260922
    kind: result
    commit: "64a7b183"
    note: "Cephalexin (PubChem CID 27447, C16H17N3O4S, 24 heavy atoms) on CUHK Charles, Slurm 2142880, Agent goal g3, five GFN engine calls. The same database conformer relaxed two ways, then a GFN2-xTB Hessian on each. At the GFN2 minimum: max|g| 9.4255e-05 Eh/Bohr, 117 real modes, and a carbonyl assignment made from per-atom mode participation -- beta-lactam mode 99 at 1866.33 cm-1 (C10+O1, 0.969), side-chain amide mode 97 at 1752.42 cm-1 (C14+O2, 0.980), carboxyl mode 98 between them -- separation +113.9 cm-1, host RRHO G(298.15 K, 1 atm) -70.959961 Eh against xTB's own printed -70.955052 Eh, 3.1 kcal/mol apart on low-mode treatment alone. At the GFN-FF minimum, itself converged to |g|norm 1.5253e-04 on its own surface: max|g| 0.024747 Eh/Bohr, 55x the optimiser's criterion, three imaginary modes (-56.1, -35.6, -22.1 cm-1) and the strongest carbonyl band 111.6 cm-1 lower at 1754.7. The cheap geometry costs 17.77 kcal/mol of GFN2 energy and lands its beta-lactam band nearer the experimental 1760 cm-1 than the true minimum does -- a right-looking number at a point that is not stationary, which is the whole reason the gradient has to be measured."
    falsifier: "A GFN2 Hessian taken at a geometry optimised by a different Hamiltonian reports a max|g| under 4.5e-4 Eh/Bohr, or the host records no gradient for an xTB Hessian that wrote one."
  - id: negative.a_gfnff_result_cannot_state_its_own_electronic_state
    kind: negative_result
    commit: "64a7b183"
    note: "Both GFN-FF nodes of goal g3 ran to normal termination and were then failed by the host with xtb.result.settings_mismatch: settings.charge expected 0 observed None, settings.multiplicity expected 1 observed None. The cause is not the request -- the argv carried '--chrg 0 --uhf 0' -- but the reading: xTB prints the 'net charge' and 'unpaired electrons' lines in the SCC setup block, and GFN-FF, having no SCC, prints no such block. XTBMainOut parses exactly those two lines. So gfnff is in the declared gfn_version domain, is advertised through inspect_program and executes, yet no GFN-FF result can pass ChemSmart's own result validation. The session worked around it and spent both admitted revisions doing so, and the goal settled exhausted."
    falsifier: "A GFN-FF xTB 6.7.1 output prints 'net charge' and 'unpaired electrons' in a block XTBMainOut reads, or the result validator passes a GFN-FF run without them."
  - id: negative.an_analysis_chain_cannot_plan_a_mode_it_has_not_read
    kind: negative_result
    commit: "64a7b183"
    note: "The approved analysis chain is planned before execution, but assigning a carbonyl band requires reading that Hessian's own per-atom participation after it. In goal g3 the session assigned the two bands on the first Hessian from measured participation and then blocked five analysis nodes on the second with its own reason: 'Mode selection is a post-execution data read; with no further agent cycle after this wave, the adaptive assignment cannot be planned as a claim chain before execution.' It refused to reuse mode indices across the two Hessians because the carboxyl C=O sits between the other two, which is correct and is what made the comparison unplannable. One thermochemistry node then failed on the non-stationary Hessian and three further nodes skipped behind it, so the delivered report carries one route's assignment and the other's raw evidence."
    falsifier: "An analysis chain selects a normal mode by a property measured on the same result the chain is planned against, without a further agent cycle."

  - id: negative.a_constraint_with_no_value_sent_the_science_to_a_scan
    kind: negative_result
    source: {path: chemsmart/agent/tool_specs.py, anchor: "value the bound geometry already has"}
    note: "ORCA modred was executable, readable and reachable, and the science still went elsewhere. Told only that constrained coordinates are held fixed while everything else relaxes, a live session (CUHK r9o-g1, 2026-09-21) asked to relax BINOL with its biaryl torsion brought coplanar read that as moved there and held, wrote the value it meant into a modred project YAML as `value: 0.0` -- a key the loader does not have -- had it correctly rejected, and replanned the stage as a relaxed scan, the only coordinate vocabulary that carries a target. A capability is not reachable until the one fact that decides how to use it is written where the model reads it."
    falsifier: "A session asked to relax a structure at a coordinate value it does not yet have plans a constrained optimisation without first moving the coordinate, or invents a target value for a constraint."
  - id: lesson.a_refusal_that_names_no_route_is_a_route_not_taken
    kind: lesson
    source: {path: chemsmart/agent/commands.py, anchor: "the job types of this program that do are"}
    note: "With the constraint sentence in place the next session (r9o-g3) took the right first step -- set_dihedral onto the coordinate -- then attached the coordinate to an opt node and met `live Click scope run/orca/opt has no coordinates option`. True, protective, and silent about where a coordinate may go: the session dropped the coordinate and compiled a plain optimisation, which relaxes straight off the coordinate it had just been asked to hold. With the refusal naming the job types the live Click tree says carry the option, the next session compiled modred --coordinates instead. Two streams, one seam, and the message was the whole difference."
    falsifier: "A session that meets a refusal naming a legal route takes it no more often than one that meets the same refusal without it."
  - id: negative.a_rigid_turn_to_coplanarity_is_a_bad_seed_for_a_constrained_optimisation
    kind: negative_result
    source: {path: .agents/charter/orca-scan-irc-modred.md, anchor: "edit the coordinate there first"}
    note: "The composition the host names -- edit the coordinate, then constrain it -- is legal and was taken, and for a 106 degree turn it seeds the optimisation from a structure the molecule cannot adopt. On BINOL, set_dihedral to 0 degrees left H-H at 0.314 A and the host said so on its edit receipt. ORCA then held the torsion exactly (-1.4e-5 degrees measured at cycle 103) and spent 103 cycles and the whole approved 4 h node budget relieving 1560 kcal/mol of that strain, reaching -919.8208 Eh while still descending 0.26 kcal/mol per cycle, 60.5 kcal/mol above the free minimum, with max step 24x tolerance. The constraint worked; the seed did not."
    falsifier: "A constrained optimisation seeded by a large rigid rotation converges in a cycle count comparable to one seeded from a neighbouring point of a relaxed scan."
  - id: negative.a_gaussian_td_stage_borrows_no_level_of_theory
    kind: negative_result
    source: {path: "chemsmart/jobs/settings.py", anchor: "check if td settings exist (optional)"}
    note: "Gaussian `td` looked like the round's next qualification-only cell -- 28 declared selectors, the classification verified (Gaussian16Output.jobtype overrides a route-derived `sp` when the route carries a response block), 24 of 28 selectors resolving on the archived 50-state CAM-B3LYP result, the stage resolving to one result so an extraction plans, and the host's own verifier already checking that the engine returned at least `nstates` transitions. The flag is the only code between it and qualification. What is not qualification-only is the project shape. `molecular_project_section_sources(jobtype='td')` answers `('td',)`, so a td stage borrows nothing from `gas:` or `solv:` the way every other stage does; the phase loop skips it by name and its own branch reseeds from stage defaults. A project that names the level of theory once, in `gas:`, and asks `td: {nstates: 12}` therefore builds a route with no method and dies inside the writer on `ValueError: No computational method provided` -- not a typed refusal, and identical in kind to the defect the `sp` branch immediately above records for a gas-only project. Repeating the method inside `td:` works and was verified (`# cam-b3lyp 6-31g(d) TD(singlets,nstates=12,root=1)`), so the cell is executable today. Making the borrow real would move a declaration ORCA shares, which is why this round stopped here rather than widening."
    falsifier: "A Gaussian project naming the level of theory only in `gas:` and a `td:` section naming only response settings compiles a TD route carrying that level."
  - id: negative.a_gaussian_number_carries_no_electronic_state
    kind: negative_result
    source: {path: "chemsmart/analysis/result_readers.py", anchor: "selector_electronic_provenance"}
    note: "The Gaussian reader declares no electronic provenance, so every value it delivers reaches the extraction receipt `stateless` -- on a TD result an excitation energy and a reference orbital eigenvalue arrive indistinguishable, which is that job type's whole scientific content. ORCA declares 38 rows and PySCF 43. The axis cannot be declared partially: the cross-program contract requires any declaring reader to cover the mean-field set as well as the excitation set (`dipole_moment` and `scf_energy` as `reference`, `energy` as `computed_surface`) and to supply a resolver, so that a ground-state number cannot sit silent beside excited-state ones. Gaussian cannot meet it as it stands: it serves no `scf_energy` selector, its parser exposes no followed root and no surface record, and its `density` keyword can move whose density a dipole or a population is. Declaring the axis therefore costs a parser property, a resolver and a new selector -- a coherent piece of work, and a different one from qualifying `td`."
    falsifier: "The Gaussian reader declares the provenance axis and the cross-program contract passes, or a consumer is shown to need no provenance from a TD result."

  - id: negative.a_categorical_selector_cannot_be_a_delivered_observable
    kind: negative_result
    commit: "b5c07fc7"
    note: "Found by the live stability goal (CUHK Slurm 2145043), which settled unreachable_from_evidence over a reader that worked. declare_requested_observable takes a unit from the typed vocabulary and the completion gate requires, per declared observable, a delivered claim in its dimension; a word-valued selector resolves fine and then fails numeric rendering -- the host's own words, 'numeric extraction fails with could not convert string to float: stable'. So scf_stability_internal/external, and equally solvation_model, solvent, irc_direction, functional, basis and every other categorical selector, can be extracted and can ride a receipt but can never be what a goal delivers. The session was not wrong to want a number: PySCF's stability analysis has eigenvalues and this host records only the boolean. The refusal itself behaved correctly and delivered the verdict inside the recorded decision instead of converting a word to a number."
    falsifier: "A goal settles achieved on a declared observable whose delivered claim is a categorical selector's value, or declare_requested_observable admits an observable with no unit."
  - id: result.pyscf_reference_stability_selectors_20260922
    kind: result
    commit: "b5c07fc7"
    note: "status/properties/scf_stability was recorded from contract v7 and read only by the sensor that raises scf.reference_unstable; three selectors now serve it. Exercised on ten archived real fixtures: water RKS stable/stable; singlet O2 at RKS and at RHF internally stable and externally unstable about RHF/RKS -> UHF/UKS; triplet O2 at UKS externally unstable about UHF/UKS -> GHF/GKS, the same word for different physics, which is why the rotation space is a selector and not a display detail; an ROHF hydrogen atom whose external question PySCF cannot answer while its internal one is served; and an unconverged run where both come back unstable on orbitals that are not stationary. Live (Slurm 2145043) the DANS RKS reference at CAM-B3LYP/def2-SVP is stable on both questions, at a total energy reproducing the producing optimisation to 5e-12 Eh, so the previous goal's spectra rest on a sound reference. Not qualified: no extraction receipt bound the selectors and the host wrote no qualification row."
    falsifier: "A PySCF artifact recording an external stability answer whose rotation_space does not follow its reference family, or a reader that answers scf_stability_external on an ROHF reference."

  - id: result.orca_modred_node_ran_to_maxiter_and_seeded_a_saddle_20260923
    kind: result
    source: {path: .agents/charter/orca-scan-irc-modred.md, anchor: "modred"}
    note: "CUHK Slurm 2144929 (r9o-g5, BINOL 22 heavy atoms, B3LYP-D3(BJ)/def2-SVP, 64 ranks), task byte-identical to g4, code 4d70e6c0 plus the uncommitted modred flag (tree digest 9e1f919a). Cycle 1: the relaxed scan died on step 1 after 24672 s with ORCA's own 'ERROR (SHARK): Failed to read input file' (typed failed_nonconverged_scan_step; not a timeout). Cycle 2: the session chose one modred node holding D(C12,C11,C10,C1) at 0 deg; it terminated normally after 108 cycles = ORCA's MaxIter 3N, torsion held at 0.00 deg, NOT converged (no HURRAY), 19578 s, E = -919.866121 Eh, 32.0 kcal/mol above the minimum; the host verified and extracted it and its structure crossed the producer edge into cycle 3: OptTS Freq converged (HURRAY) in 16453 s, peak RSS 56.9 GB, one imaginary mode -137.51 cm^-1 in the final block, E = -919.904757 Eh, 7.8 kcal/mol above the minimum. The settle phase claimed achieved_with_observations in prose without receipts and the host returned the goal to the human. Zero tracebacks; the repaired kill path was not exercised. No qualified row was written; the modred execution flag stays withdrawn on the merged tree."
    falsifier: "A g5 receipt in which the modred node's constraint table shows the held torsion away from 0.00 deg, or an ORCA OptTS final frequency block for cycle 3 with a count of imaginary modes other than one."
# Annotations keyed by DERIVED rule_id; no rule text is repeated here.
# class: FUNDAMENTAL | MECHANICS (how to drive a tool) | SCIENCE (what must
# stay true about chemistry) | AUTHORITY | EARNED (names its loss).
annotations:
  stem.never_author_native: {class: FUNDAMENTAL, backstop: {gate: CONDUCT.md section 2 "no model-authored native input, path, shell, or status"}, ablate: never}
  stem.host_renders_claims: {class: FUNDAMENTAL, ablate: never, note: "record_analysis_claims binds values, but nothing stops a model *reporting* an unbound number"}
  stem.validation_is_typed:
    class: MECHANICS
    backstop: {refusal: validation_requires_typed_analysis, commit: "00002087"}
    falsifier: With the sentence removed, a session that reaches a scientific_validation node still ends with a typed verdict, at a cost of at most one refused call.
    last_validated: null
  stem.receipts_travel_typed: {class: MECHANICS, backstop: {gate: tool.dispatch.rejected}, falsifier: "Removed, sessions still bind the right receipt within two refused calls.", last_validated: null}
  stem.plan_repair_and_inputs: {class: MECHANICS, backstop: {refusal: plan findings returned by plan_scientific_workflow}, falsifier: "Removed, plans still reach `planned` with no more than two extra planning calls.", last_validated: null}
  stem.amend_not_resubmit: {class: MECHANICS, backstop: null, falsifier: "Removed, repair costs more turns but no plan is lost.", last_validated: null}
  leaf.pyscf.a_hessian_is_the_curvature_of_the_surface_it_names:
    class: SCIENCE
    backstop: {refusal: "has no analytic Hessian of an excited root"}
    falsifier: "A session that optimises on an excited root and is asked for that state's equilibrium structure plans no Hessian on that root, or one without the root's response settings."
    last_validated: "2026-09-19"
  leaf.pyscf.an_irc_is_one_branch_from_a_saddle_of_its_own_surface:
    class: SCIENCE
    backstop: {refusal: "PySCF irc walks one branch per node"}
    falsifier: "A session asked which minima a saddle connects plans one PySCF irc node only, or reads forward and backward as reactant and product without reading where each branch ended."
    last_validated: null
  leaf.pyscf.a_converged_reference_can_be_a_saddle:
    class: SCIENCE
    backstop: null
    falsifier: "Removed, sessions working on a singlet with diradical character or on an HF reference under a correlated method ask for scf_stability no less often."
    last_validated: null

edges:
  - {from: fundamental.hub, to: stem.never_author_native, rel: expressed_by}
  - {from: invariant.claim_ladder, to: charter.scientific_invariants, rel: duplicates}
  - {from: charter.product_boundary, to: invariant.gates, rel: depends_on}
  - {from: leaf.pyscf.a_hessian_is_the_curvature_of_the_surface_it_names, to: commit.44499f2a, rel: depends_on, note: "replaces leaf.pyscf.an_excited_minimum_has_no_hessian_here, which denied what 44499f2a shipped; the new sentence carries settings boundaries asked of the project loader"}
  - {from: commit.ec1f5ba5, to: result.xtb_mode_tables_agree, rel: depends_on}
  - {from: leaf.pyscf.a_hessian_is_the_curvature_of_the_surface_it_names, to: result.pak_campaign_pyscf_20260919, rel: evidenced_by, note: "formaldehyde and trans-glyoxal each planned the root's Hessian with the root's settings; only formaldehyde's ran"}
  - {from: leaf.pyscf.a_converged_reference_can_be_a_saddle, to: result.pak_campaign_pyscf_20260919, rel: evidenced_by, note: "asked where the sentence names (twisted ethylene, ozone's RHF under MP2); the falsifier compares against an arm without the sentence, which this campaign did not run, so it stays unvalidated"}
  - {from: leaf.pyscf.an_irc_is_one_branch_from_a_saddle_of_its_own_surface, to: negative.geometric_forward_is_a_sign, rel: depends_on, note: "the branch word is a host-fixed sign because geomeTRIC's word is not a direction"}
  - {from: leaf.pyscf.an_irc_is_one_branch_from_a_saddle_of_its_own_surface, to: negative.geometric_kernel_irc_never_steps, rel: depends_on, note: "the driver walks geomeTRIC with PySCF's engine itself"}
  - {from: leaf.pyscf.an_irc_is_one_branch_from_a_saddle_of_its_own_surface, to: result.pyscf_irc_campaign_20260920, rel: evidenced_by, note: "every session that walked branches read each endpoint's connectivity before naming it; methoxy declined to walk from another surface's saddle. No arm ran without the sentence, so it stays unvalidated"}
  - {from: result.pyscf_ts_cli_20260921, to: negative.a_converged_ts_search_can_be_a_minimum, rel: evidenced_by}
  - {from: result.pyscf_ts_cli_20260921, to: negative.geometric_updated_hessian_is_not_an_order, rel: evidenced_by}
  - {from: lesson.an_order_is_a_property_of_a_stationary_point, to: invariant.gates, rel: depends_on}
  - {from: lesson.an_order_is_a_property_of_a_stationary_point, to: negative.mode_count_certified_a_non_stationary_saddle, rel: supersedes}
  - {from: result.an_irc_endpoint_reached_by_minimising, to: result.irc_path_is_not_everywhere_steepest_descent, rel: depends_on}
  - {from: lesson.a_promise_belongs_to_the_structure_not_the_node, to: negative.a_converged_ts_search_can_be_a_minimum, rel: evidenced_by}
  - {from: result.gaussian_ran_through_chemsmart_on_cuhk_20260921, to: negative.a_relaxed_scan_classified_itself_as_a_single_point, rel: evidenced_by}
  - {from: negative.a_relaxed_scan_classified_itself_as_a_single_point, to: lesson.a_classification_written_into_a_setter_nothing_reads_was_never_made, rel: depends_on}
  - {from: negative.a_duplicated_route_keyword_dropped_the_hessian_silently, to: result.gaussian_ran_through_chemsmart_on_cuhk_20260921, rel: evidenced_by}
  - {from: negative.a_green_gaussian_review_was_never_decided, to: result.gaussian_ran_through_chemsmart_on_cuhk_20260921, rel: depends_on}
  - {from: lesson.two_organs_answer_the_reached_structure_and_one_asks_the_reader, to: negative.a_relaxed_scan_classified_itself_as_a_single_point, rel: depends_on}
  - {from: result.gaussian_executed_under_the_approval_chain_20260921, to: negative.a_green_gaussian_review_was_never_decided, rel: supersedes}
  - {from: result.pyscf_decomposition_cli_20260921, to: lesson.an_absence_names_which_absence_it_is, rel: evidenced_by, note: "the four fixtures are the four absences and the two presences; each refusal names which one it met"}
  - {from: negative.a_predicate_read_as_a_value_validates_nothing, to: lesson.a_summary_nothing_rederives_is_not_evidence, rel: depends_on, note: "the same class one layer down: there a summary nothing re-derived, here a re-derivation that compared booleans"}
  - {from: commit.88a6051d, to: result.xtb_solvation_terms_20260921, rel: depends_on}
  - {from: commit.88a6051d, to: negative.every_archived_xtb_run_has_solvation_off, rel: supersedes}
  - {from: result.xtb_solvation_terms_20260921, to: negative.every_archived_xtb_run_has_solvation_off, rel: evidenced_by, note: "the archived toluene runs proved the sentence false; these live water and methane runs are what the declaration was then qualified on"}
  - {from: commit.88a6051d, to: negative.an_xtb_hessian_matrix_has_no_consumer, rel: explains, note: "the round that declared the solvation surface looked at the Hessian matrix and recorded why it stays unreachable instead of adding a selector for it"}
  - {from: result.xtb_gfnff_solvation_is_undecomposed, to: result.xtb_solvation_terms_20260921, rel: depends_on, note: "the same run set; this is the one member whose decomposition is incomplete, and it is what says the closure check must be conditional"}
  - {from: commit.f7e39390, to: result.xtb_saddle_escape_20260921, rel: depends_on}
  - {from: commit.f7e39390, to: lesson.a_helper_written_and_never_called_is_a_dead_route, rel: evidenced_by}
  - {from: lesson.a_helper_written_and_never_called_is_a_dead_route, to: negative.two_organs_hand_a_reader_two_types_for_one_path, rel: depends_on, note: "both found by the same live goal walking the same route: one stopped the step, the other stopped the characterisation of what was stepped off"}
  - {from: result.orca_irc_path_and_endpoints_20260921, to: negative.an_engine_written_sidecar_is_not_a_registered_geometry, rel: depends_on, note: "the path had to come from the program's own reader because nothing registers what an engine wrote"}
  - {from: result.orca_irc_path_and_endpoints_20260921, to: lesson.a_path_string_is_not_a_file_identity, rel: depends_on, note: "the three branches ran only after the staging guard stopped comparing path strings"}
  - {from: leaf.saddle.orca_irc_walks_one_direction, to: result.orca_irc_path_and_endpoints_20260921, rel: evidenced_by, note: "the direction-both table opens on an endpoint, not on the saddle; no arm ran without the sentence, so it stays unvalidated"}
  - {from: negative.i_read_the_wrong_hessian_block_by_eye, to: result.orca_irc_path_and_endpoints_20260921, rel: depends_on, note: "the campaign's own reference numbers were read by eye once and were wrong once"}
  - {from: result.gaussian_ts_and_irc_executed_under_the_approval_chain_20260921, to: negative.a_gaussian_irc_node_cannot_say_which_way_it_walks, rel: evidenced_by, note: "the goal that earned the two job types is the one whose two direction projects were refused"}
  - {from: result.gaussian_ts_and_irc_executed_under_the_approval_chain_20260921, to: result.gaussian_executed_under_the_approval_chain_20260921, rel: depends_on, note: "opt and sp on this target came first; ts and irc are the same chain one stage further"}
  - {from: lesson.a_refusal_names_an_act_not_an_eventual_state, to: result.gaussian_ts_and_irc_executed_under_the_approval_chain_20260921, rel: evidenced_by, note: "both live goals paid two provider turns at the same sentence"}
  - {from: result.pyscf_response_permittivity_is_waters, to: negative.a_recorded_response_dielectric_had_no_reader, rel: evidenced_by, note: "the divergence the fixture was written to show, read for the first time through a selector rather than asserted on the raw stage record"}
  - {from: negative.a_recorded_response_dielectric_had_no_reader, to: lesson.a_classification_written_into_a_setter_nothing_reads_was_never_made, rel: depends_on, note: "one layer out: there a value the host resolved and discarded, here two values the host stored and nothing on the reader plane asked for"}
  - {from: result.pyscf_dans_solvatochromism_20260922, to: negative.a_recorded_response_dielectric_had_no_reader, rel: evidenced_by, note: "the first live goal in which the two permittivities reached a session at all; they arrived on the level record of every extraction receipt"}
  - {from: negative.the_shift_was_delivered_without_the_permittivity_that_bounds_it, to: result.pyscf_dans_solvatochromism_20260922, rel: depends_on}
  - {from: lesson.an_order_is_a_property_of_a_stationary_point, to: result.xtb_cheap_hessian_is_not_a_minimum_20260922, rel: evidenced_by, note: "the sentence reached a second program: the anomaly fired and the host failed the node on result.stationary_point_order, on a gradient no xTB reader could measure before"}
  - {from: result.xtb_cheap_hessian_is_not_a_minimum_20260922, to: negative.a_gfnff_result_cannot_state_its_own_electronic_state, rel: depends_on, note: "the cheap route is GFN-FF, so the goal could not reach its own question without meeting the reader limit that fails every GFN-FF result"}
  - {from: result.xtb_cheap_hessian_is_not_a_minimum_20260922, to: negative.an_analysis_chain_cannot_plan_a_mode_it_has_not_read, rel: depends_on, note: "both Hessians ran; what the goal could not deliver was the second route's assignment, and the session said why in its own blocking reason"}
  - {from: negative.a_constraint_with_no_value_sent_the_science_to_a_scan, to: lesson.a_refusal_that_names_no_route_is_a_route_not_taken, rel: depends_on, note: "the same seam two streams apart: first nothing said what a constraint holds at, then nothing said which stage may carry one"}
  - {from: negative.a_rigid_turn_to_coplanarity_is_a_bad_seed_for_a_constrained_optimisation, to: negative.a_constraint_with_no_value_sent_the_science_to_a_scan, rel: depends_on, note: "the edit-then-constrain route exists because a constraint carries no value; its cost on a large turn is what that route buys"}
  - {from: result.pyscf_reference_stability_selectors_20260922, to: negative.a_recorded_response_dielectric_had_no_reader, rel: depends_on, note: "the same shape one surface over: a value the host recorded and one organ read, invisible to extraction"}
  - {from: negative.a_categorical_selector_cannot_be_a_delivered_observable, to: result.pyscf_reference_stability_selectors_20260922, rel: depends_on}
  - {from: result.orca_modred_node_ran_to_maxiter_and_seeded_a_saddle_20260923, to: negative.a_rigid_turn_to_coplanarity_is_a_bad_seed_for_a_constrained_optimisation, rel: depends_on, note: "twice the node budget of g4 and the same seed: ORCA's cycle limit, not the clock, ended it, with the constraint held and the geometry still descending"}
  - {from: result.orca_modred_node_ran_to_maxiter_and_seeded_a_saddle_20260923, to: negative.a_constraint_with_no_value_sent_the_science_to_a_scan, rel: depends_on, note: "the session reached modred only after a scan it chose first had failed; what it delivered was returned because the settle phase spoke in prose"}
