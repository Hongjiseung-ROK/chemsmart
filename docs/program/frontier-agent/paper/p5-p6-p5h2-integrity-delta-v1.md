# P5/P6 P5H-v2 Integrity Delta v1

## Status

Closed blocked delta. This append-only P6 delta records the qualified P5H-v2
pair-ownership control after the existing P6 candidate closure. It preserves
every frozen P5 and P6 artifact, creates no held-out content or trial, and
cannot change any P5/P6 no-go decision. Its focused offline test invoked the
dedicated validator and passed.

## Objective

The P6 candidate closure is a time-bounded reference package. P5H-v2 later
found and refused a cross-group synthetic pair-commitment reuse, while also
recording that P5H-v1's broad historical static guard is currently red because
later fixture-only harness consumers import its helper. This delta records both
facts without rewriting history or treating either as held-out evidence.

## Inputs

| Input | Required use |
| --- | --- |
| P5H-v2 receipt | Bind the qualified fixture result, failure ledger, zero authority, source hashes, and unchanged ineligibility. |
| Frozen P5 preregistration and close receipt | Require zero live trials and preserve every original P5 red gate. |
| Frozen P6 no-go manifest, close receipt, and candidate closure | Preserve no release, replication, training, or SOTA conclusion. |

## Tools and budget

Allowed tools are local JSON parsing, SHA-256, and one focused offline test.
The budget is zero provider calls, held-out accesses, tool dispatches, engines,
schedulers, network calls, installs, commits, pushes, training runs, or
publication actions.

## Gate reconciliation

| Surface | Status | Boundary |
| --- | --- | --- |
| P5H2 global pair ownership | Fixture-supported only | Reuse across distinct opaque case × repetition groups is refused. |
| P5H2 predecessor preservation | Qualified | Exact v1 source/receipt bytes remain, but its broad current static scan is red and recorded. |
| P5 evaluation | Red unchanged | No independent custody, active executor, raw trial, score, or interval exists. |
| P6 replication/training/release/SOTA | No-go unchanged | The delta supplies only local fixture provenance. |

## Failure and decision ledger

| ID | Failure or decision | Hypothesis / minimal action | Evidence and limitation | Rollback boundary |
| --- | --- | --- | --- | --- |
| P56H2-F1 | The prior candidate closure predates P5H-v2. | Add a separate hash-linked delta rather than editing its static manifest. | The delta binds P5H-v2 plus frozen P5/P6 no-go sources; it is not a reconstructed closure. | Delete only this delta; preserve the prior closure and all frozen inputs. |
| P56H2-F2 | A historical P5H-v1 static guard is red on current revalidation. | Keep its exact bytes, carry the qualified limitation forward, and prohibit promotion. | P5H-v2 records the failure; this does not establish active-path safety. | Replace only through a reviewed future guard, never by rewriting P5H-v1. |

## Claims

- Supported: P5H-v2 locally rejects its declared synthetic pair-reuse case.
- Qualified: P5H-v1 bytes are preserved while its current broad static rerun is red.
- Unresolved: external custody, active executor enforcement, model/tool-loop behavior, chemical-result provenance, trials, replication, training, release, and SOTA.
- Rejected: that this delta enables P5, alters P6 no-go, or supports a scientific/comparative result.

## Phase-close validation

The focused delta test passed `1` test in `0.04 s`; it invoked the dedicated
validator. This proves only that the new receipt, its sources, and unchanged
P5/P6 gates are locally consistent.
