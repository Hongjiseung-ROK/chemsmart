# M4 goal command

~~~text
Goal: complete only M4 of the ChemSmart PRP-10 adaptive API program: defect-driven one-factor experiments until an evidence-based adaptive terminal condition.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, docs/evaluation/frontier-agent-ablation-protocol.md, docs/goals/prp10-adaptive-api/M4-defect-driven-adaptive-expansion.md, and verified M0-M3 receipts. Re-audit branch/dirty state, frozen baseline, failure taxonomy, experiment stream, quota state, graders, and review packets. Preserve historical evidence and unrelated work. Opened PRP-10-V1 papers are development data, not a still-held-out confirmation set.

Prioritize severe false-ready, molecular identity/electronic-state/provenance failures, recurrent causal stages, repairable validator gaps, critic false negatives/positives, and context/tool/handoff loss. Bind each initial request or retry to a registered unique hypothesis/case ID, comparator, exactly one changed component, expected result, deterministic oracle, source/prompt/tool/configuration hashes, and novelty reason; a retry retains the case and records its attempt reason. Hold papers, prompts, tools, order, adaptive network envelope, and validators fixed. Grade deterministically, obtain independent cross-examination, then retain, revise, or retire. Extend supported effects to a different chemistry domain. Repeat only for preregistered uncertainty, critic recall, or reproducibility.

Use current user quota with transport_attempt_limit=None; calls are metrics, not a target. Stop on quota exhaustion, no unique verifiable hypothesis, credential revocation, or safety red line. Never top up, burn quota, route around provider failure, expose secrets, or run Gaussian/ORCA/xTB/HPC. Every retained change requires zero approval/native-input/artifact/secret bypass, zero success with a red deterministic gate, zero terminal mismatch, and deterministic replay/rerender. Report regressions and cost beside gains.

After harness freeze, run the full agent suite, read-only Ruff, replay/schema, citation/license/link/secret, and diff gates once each; no autofix or snapshots. Report change, empirical result, failure, unknown, retain/revise/reject, campaign terminal reason, and need for a new confirmatory lockbox. Commit phase-owned changes and fetch-first non-force push to fork. No PR, release, paper submission, or next-phase training.
~~~
