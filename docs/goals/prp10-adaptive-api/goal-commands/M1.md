# M1 goal command

~~~text
Goal: complete only M1 of the ChemSmart PRP-10 adaptive API program: exact official coordinate provenance and private safe-preview custody.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, docs/evaluation/frontier-agent-ablation-protocol.md, docs/goals/prp10-adaptive-api/M1-coordinate-and-preview-custody.md, and verified M0 receipts. Inspect the actual branch, dirty state, coordinate/artifact contracts, CLI safe-preview path, evidence storage, and focused tests. Preserve unrelated work and historical receipts.

Act as a transparent computational-chemistry researcher and ChemSmart agent developer. For PRP-10 eligibility accept only exact official single-frame XYZ in angstrom. Implement and validate CoordinateImportReceipt binding source locator/archive member, source and imported-byte SHA-256, frame count, units, atom order, molecular identity approval, and access/license record. Do not infer identity from a filename, transcribe SI coordinate tables, use OCR, generate 3D from SMILES, or let a model create/repair coordinates. Do not overwrite user artifacts. Missing coordinates must block dependent nodes with evidence-linked reasons while unrelated source work may continue. SDF/MOL/PDB conversion is a separate deterministic general-input feature and does not satisfy PRP-10 eligibility.

Retain the exact ChemSmart-generated safe-preview bytes in a private evidence store and expose only permitted metadata and exact-byte hashes publicly. The model may repair typed scientific intent or approved project YAML, never Gaussian/ORCA/xTB native input. Perform safe fake/test preview only; run zero chemistry engines, schedulers, and HPC jobs. A new artifact may reach previewed, not executed or reproduced.

Use positive and adversarial fixtures for byte identity, atom order, units, source binding, non-overwrite, multiframe rejection, and missing-coordinate blocking. Run one focused M1 suite and at most one evidence-driven rerun; no broad pytest/Ruff, autofix, or snapshots. Report change, empirical result, failure, unknown, retain/revise/reject, and M2 entry gate. Commit only phase-owned changes and fetch-first non-force push to fork after verification. No PR; do not continue to M2.
~~~
