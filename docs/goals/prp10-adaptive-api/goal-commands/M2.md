# M2 goal command

~~~text
Goal: complete only M2 of the ChemSmart PRP-10 adaptive API program: an independent, replayable ten-switch ablation plane.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, docs/evaluation/frontier-agent-ablation-protocol.md, docs/goals/prp10-adaptive-api/M2-independent-ablation-plane.md, and verified M0-M1 receipts. Re-audit branch, dirty state, Runtime V2, experiment events, task packets, budgets, critics, command compiler, and focused tests. Preserve unrelated work and extend Runtime V2 additively; do not weaken production behavior.

Implement independently versioned switches for task decomposition, specialist roles, evidence retrieval, domain-knowledge packs, structured documentation, independent critic, adversarial cross-examination, bounded repair, command-DAG planning, and deterministic feedback. Retain a single-agent baseline. Record every switch plus model/provider, prompt/tool-schema digest, adaptive network envelope, source/project/command hashes, validators, artifacts, failures, repairs, usage, and terminal state in an additive hash-chained experiment stream.

Require paired cases to hold paper, prompt, tools, task order, budget, and validators fixed while exactly one factor changes. Permission, live CLI schema, artifact hashing, secret redaction, deterministic safety validators, and native-input, chemistry-engine, scheduler, and HPC prohibitions stay enabled in all conditions. When review is enabled, use exactly three fresh read-only roles: domain/paper fidelity, command/evidence integrity, and adversarial omission/state/safety. Critics cannot repair, approve, execute, or set readiness; critical disagreement needs deterministic evidence or user adjudication.

Prove switch independence, immutable safety invariants, replay/rerender determinism, and single-factor comparisons with one focused M2 suite and at most one evidence-driven rerun. No broad pytest/Ruff, autofix, snapshots, engines, or HPC. Report change, empirical result, failure, unknown, retain/revise/reject, and M3 entry gate. Commit only phase-owned changes and fetch-first non-force push to fork after verification. No PR; do not continue to M3.
~~~
