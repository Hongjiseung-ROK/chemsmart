# Copyable Goal command — v2

~~~text
Advance ChemSmart until 2026-08-02 23:59 KST as an integrity-first computational-chemistry researcher and professional agent developer. The target is a general paper-to-research-plan agent: paper/SI evidence -> typed scientific specification -> validated ChemSmart project YAML -> canonical ChemSmart command DAG -> safe preview -> deterministic validation -> independent review -> reproducible plan.

Read and obey AGENTS.md and docs/goals/frontier-live-development-2026-08-02/goal-schema-v2.json. Inspect repository state and evidence before mutation; preserve unrelated work. Use English for all task artifacts.

Continue from sealed evidence. Registry V2 has 1,771 loader/renderer-observed settings but no engine or suitability authority. SxK supports registry exposure as a candidate; packs remain narrow and opt-in. V5r2 recovered 5/5 archived validator failures; V5r3 completed exact inspect -> reject -> one-field repair -> accept for 4/4 seeded defects. This is development-case harness evidence only.

Run two coordinated tracks. First, use an external content-addressed challenge rather than Python fixture tables to test unseen Gaussian, ORCA, and xTB settings: embedded-dispersion functionals, multifield composition, basis-role conflict, ORCA native-basis routing, double-dispersion conflict, unknown xTB generations, method-solvent coverage, and an irrelevant-pack control. Inspect exact English responses, grade only typed actions and deterministic evidence, repair causal defects, and rerun only changed hypotheses.

Second, run a coordinate-independent full-text campaign. Add typed Crossref metadata/BibTeX receipts, Unicode character-range locators, and bibliographic concordance. Use DeepSeek on preregistered program/version, DFT, stationary-point, open-shell, correlation, and unsupported-IRC claims. Search APIs are discovery-only; freeze gold locators first. Maximum state: validated_source_evidence.

Use DeepSeek V4 Flash only, with thinking enabled and continuation never treated as evidence. All remaining current user-owned model and literature quota is authorized without per-call reapproval or a fixed attempt cap. Every request/retry needs a unique hypothesis, comparator, changed factor, expected observation, deterministic oracle, and source/prompt/tool/configuration/budget hashes. Never top up, bypass failure, expose secrets, or repeat calls without scientific purpose.

Keep the model command-first. It must not author or edit Gaussian, ORCA, or xTB native input, raw shell commands, or coordinates. ChemSmart owns setting resolution, project rendering/loading, CLI-schema compilation, artifact binding, safe preview, validation, approval, and terminal state. Run zero chemistry engines, schedulers, and HPC jobs. Missing critical evidence stays blocked. Do not begin coordinate-bound paper planning until the user explicitly resolves allowed coordinate sources and formats plus identity, units, charge, multiplicity, and import behavior.

Prefer real English model-response inspection and deterministic receipts over repetitive tests. Use one focused suite per substantial milestone and at most one evidence-driven rerun; run broad agent, read-only Ruff, replay/schema, citation/license/link/secret, and diff gates only after harness freeze. Record empirical effects, failures, unknowns, and retain/revise/reject decisions. Make reviewable Conventional Commits, fetch before non-force pushes to fork, verify SHAs, and open no PR. Stop only at the deadline, explicit DeepSeek quota exhaustion, credential revocation, no unique verifiable hypothesis, or a safety red line.
~~~
