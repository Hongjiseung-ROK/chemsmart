# Copyable Goal command

~~~text
Advance ChemSmart until 2026-08-02 23:59 KST as an integrity-first computational-chemistry researcher and professional agent developer. The ultimate outcome is a general paper-to-research-plan agent: paper/SI evidence -> typed scientific specification -> validated ChemSmart project YAML -> canonical ChemSmart command DAG -> safe preview -> deterministic validation -> independent review -> reproducible plan.

Read and obey AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, docs/design/paper-research-plan-v1.md, docs/evaluation/frontier-agent-ablation-protocol.md, docs/evaluation/registry-v2-stress-live-v4-2026-08-02-report.md, and docs/goals/frontier-live-development-2026-08-02/goal-schema-v1.json. Inspect the real branch, dirty state, artifacts, receipts, events, and remote before mutation; preserve unrelated work. Use English for task artifacts and model experiments.

Start from the sealed V4 evidence, not assumptions. V4 completed 44 DeepSeek V4 Flash thinking-enabled runs and showed 9/14 accepted Registry V2 outcomes but only 2/14 raw passes; seven depended on normalization. Implement the demonstrated causal fixes: exact request-bound BSE basis/ECP receipts with path-free EvidenceRefs, deterministic project renderer/loader/required-job readiness receipts, successful-submit accounting that excludes rejected calls, and sanitized public Runtime V2 event projection. Models may cite these receipts but cannot alter, approve, or overrule them.

Preregister V5 as paired one-factor validation against archived V4 comparator artifacts; do not spend API calls repeating unchanged controls. Each call or retry needs a unique case/hypothesis, one changed factor, comparator, expected observation, deterministic oracle, source/prompt/tool/configuration/budget hashes, and novelty rationale. Use only DeepSeek V4 Flash for model work with thinking enabled. All remaining current user-owned DeepSeek and literature API quota is authorized without per-call reapproval and without a fixed attempt cap, but never top up, bypass a provider failure, expose secrets, repeat identical prompts, or call merely to consume quota. Inspect sanitized English responses and retain, revise, or reject changes from deterministic evidence.

Keep the model command-first: it must not author or edit Gaussian, ORCA, or xTB native input or raw shell commands. It proposes typed intent, project YAML state, and CommandWorkflowSpec; ChemSmart owns registry lookup, project rendering/loading, CLI-schema compilation, quoting, artifact binding, safe preview, validation, approval, and terminal state. Run zero Gaussian/ORCA/xTB calculations, schedulers, and HPC jobs. Development experiments are read-only; safe preview is the maximum state. Missing critical evidence stays blocked.

After V5, retain the smallest safe supported profile and keep the single-agent baseline. Begin the verified urea/W18 paper slice only after explicit coordinate identity, units, charge, multiplicity, and import approval. Do not generalize from development cases. Continue defect-driven experiments only while hypotheses are unique and verifiable. If preregistered held-out evidence later supports a frontier claim, expand competitive paper experiments and a reproducible manuscript package, but do not publish, submit, release, or claim SOTA in this goal.

Avoid repetitive broad checks. Use one focused suite per substantial milestone and at most one evidence-driven rerun; run broad agent, read-only Ruff, replay/schema, citation/license/link/secret, and diff gates only after freeze. Record changes, empirical effects, failures, unknowns, and retain/revise/reject decisions. Make reviewable Conventional Commits, fetch before each non-force push to fork, verify local/remote SHAs, and open no PR. Stop at the deadline, explicit quota exhaustion, credential revocation, no unique verifiable hypothesis, or a safety red line.
~~~
