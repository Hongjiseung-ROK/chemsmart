# M2D goal command

~~~text
Goal: complete only M2D of the ChemSmart General Chemistry Knowledge program: run a paired settings-registry by domain-knowledge ablation and select the smallest safe non-inferior configuration.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, docs/evaluation/frontier-agent-ablation-protocol.md, docs/goals/general-chemistry-knowledge/M2D-settings-knowledge-ablation.md, and verified M2A-M2C artifacts. Re-audit branch, K0 freeze, registry/pack versions, experiment stream, graders, provider state, and focused tests. Preserve unrelated work and freeze the experiment before calls.

Preregister S0K0, S1K0, S0K1, and S1K1. Hold development papers, source windows, coordinates, prompt, model/thinking mode, task order, per-request limits, wall time, compiler, loaders, validators, graders, and reviews fixed. Change only model-visible registry and/or pack guidance. Underlying deterministic registry resolution, permissions, schema, artifact hashes, secret redaction, command compilation, safe preview, and native-input, engine, scheduler, and HPC prohibitions stay active in every arm. Keep S0K0 permanently.

Measure explicit setting preservation, critical coverage, loader-valid projects, command semantic correctness, preview success, honest blocking, false-ready, safety violations, repair, tool validity, critic recall/false rejection, tokens, cost, and latency. Never count provider-turn completion as scientific success. Use paired uncertainty intervals and expose every paper regression. Retain a component only with positive aggregate scientific coverage, a generalizable causal mechanism, and zero safety regression; otherwise revise or reject it.

Every API request or retry must bind a unique arm/case hypothesis, one changed factor, comparator, expected observation, deterministic oracle, and source/prompt/tool/configuration/budget hashes. Count is observational. Continue within current quota until exhaustion, no unique hypothesis, credential revocation, or a safety red line; no top-up, bypass, duplicate prompt, quota-burning, or secret persistence. Run zero chemistry engines/HPC and author no native input.

Run one focused M2D suite and at most one evidence-driven rerun. Report effects, uncertainty, failures, unknowns, and the exact M3 candidate. Commit only phase-owned changes and fetch-first non-force push; no PR and do not continue to M3. An ablation win is not a SOTA claim.
~~~
