# General Chemistry Knowledge Goal Commands

Copy only the text inside a phase's `~~~text` fence into a new Codex Goal.
Each command points to the ultimate-goal context and its phase document. The
machine-readable [length receipt](length-receipt.json) measures the fenced body
with its terminating newline; every command is English-only and below 3,500
Unicode characters.
