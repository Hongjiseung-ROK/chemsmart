# R2 goal command

~~~text
Goal: complete R2, provider-native DeepSeek conformance and the exact ChemSmart harness-profile experiment.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, docs/design/paper-research-plan-v1.md, docs/goals/two-frontier-paper-research/phase-status.json, and docs/goals/two-frontier-paper-research/R2-provider-harness-conformance.md. Verify R1 receipts rather than assuming completion. The retained old H0 observation is stale_invalidated and admits no current profile.

Act as a computational-chemistry expert committed to truth, transparent failure analysis, bounded expert collaboration, and a rigorous SOTA-quality paper without an unsupported SOTA claim.

Inspect branch/dirty state, provider adapters, AgentSession, UnifiedSessionRunner, ToolLoop, permissions, Runtime V2, tool registry, profile contracts, and focused tests. Keep one provider-neutral scientific kernel and provider-native wire envelopes. All profiles keep sandboxed tools, approval pause, and deterministic validator feedback on. H0 has no event replay/delegation. HC adds public event-prefix replay. HA adds progressive skills, fresh specialists, hooks, structured handoff/compaction, depth 1. HK adds persistent goal and checkpoint/fork/resume, depth 2.

Use official DeepSeek deepseek-v4-flash with thinking enabled only inside ephemeral adapter history. Preserve required reasoning_content during one uninterrupted tool turn, but never store it in public history, SessionState, events, receipts, logs, reports, or training data. Resume through a deterministic public recap or fail closed. This receipt cannot establish disabled-mode compatibility or a causal benefit from thinking.

Route a bounded command-only read/proposal tool call through the real session/runner/loop. Bind a sanitized ProviderConformanceReceipt to source snapshot, exact model/endpoint/protocol, tool schema, public history, resource budget/usage, request count, thinking mode, profile, and observed checks. Separate vendor-declared limits from observed behavior. Make zero engine/HPC calls.

Use only the current session-environment credential. Count every initial call and retry against 128 total DeepSeek transport attempts and current account quota; never use Keychain fallback, top up, print keys, persist raw provider output, change billing, or retry beyond budget. ChemSmart must not contact authors or add/propose/execute unreported sensitivity work.

After the milestone run one focused provider/session/profile/conformance suite and at most one evidence-based rerun. Ask through the question tool for material ambiguity. Report observed versus unobserved checks, cost/request receipt, sanitized failures, and R3 entry conditions. No broad tests, training, commit, or push without separate authority.
~~~
