# M5 goal command

~~~text
Goal: complete M5, the command-compiler pilot, preregistration, and integration
freeze for ChemSmart.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md,
docs/evaluation/frontier-agent-ablation-protocol.md, and
docs/goals/frontier-agent-command-refinement/{README.md,M5-pilot-and-preregistration.md}.
Act as an evidence-led computational-chemistry expert: report negative results
and uncertainty, preserve a single-agent baseline, and never call an
experimental component SOTA or default without its preregistered evidence.

Inspect all prior phase receipts before acting. Freeze model/provider revision,
prompt/skill/tool schema, CLI schema, compiler/validator, fixtures/projects,
budgets, task order, trial policy, and graders. Build only public development/
pilot fixtures. First evaluate A0 direct string versus A1 typed IR/compiler;
then estimate the 2 x 2 x 2 D/E/C study with paired repeats, deterministic
graders, bootstrap intervals, and a power calculation for an external sealed
corpus. Include command semantic, parser/preview, project/approval/artifact,
and adversarial defect metrics specified by the phase document.

DeepSeek is the sole model experiment provider; Elsevier/SerpAPI/Tavily only
verify literature through current user-owned quota and Keychain leases. Never
log keys or top up. Do not run chemistry engines/HPC/training. At the M5 freeze
only, run the full agent suite, read-only Ruff, schema/link/citation/secret/diff
checks once; no autofix/formatter/snapshot regeneration. Commit and non-force
push to fork, no PR. If an external custodian/sealed corpus is absent, prepare
the specification and stop before M6. Use the question tool when available;
otherwise ask the user directly and stop only for material unresolved authority.
~~~
