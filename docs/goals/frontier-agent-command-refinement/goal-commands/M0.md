# M0 goal command

~~~text
Goal: establish M0 of ChemSmart's command-compiled frontier-agent roadmap.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, and
docs/goals/frontier-agent-command-refinement/{README.md,M0-authority-and-command-control.md}.
Act as a computational-chemistry expert: be scientifically honest, transparent
about uncertainty, learn from failures, and use bounded advisor agents only
with typed inputs and deterministic merges. The long-term aim is a rigorous
SOTA-quality paper, never an unsupported SOTA claim.

First inspect repository, exact ancestry, branch, tracked/untracked state,
schema/runtime contracts, and relevant tests without modifying the original
checkout. Preserve the dirty precursor on an archival branch only after a
secret/copyright scan; do not print credentials. Create the active
codex/frontier-agent-command-refinement branch from the declared base.

Make the command compiler the sole route from model intent to ChemSmart argv:
models propose typed ScientificTaskSpec and CommandWorkflowSpec, never native
Gaussian/ORCA/xTB input, free-form executable shell, paths, aliases, quoting,
or fallback inputs. Update the operating contract, historical redirects, and
three local skills; add phase documents and goal prompts. Record clear evidence
of inheritance versus archival-only material.

Do not call APIs, engines, HPC, trainers, or package installers. Do not run
pytest/Ruff repeatedly; documentation-only M0 needs inspection and receipt
checks only. Use apply_patch for edits. Commit reviewed M0 changes
conventionally and non-force push to fork if the destination is unchanged; do
not open a PR. Use the question tool when available; otherwise ask the user
directly and stop only for a material unresolved permission, scientific, or
branch-ownership choice. Report exact receipts,
scope, blockers, and next phase.
~~~
