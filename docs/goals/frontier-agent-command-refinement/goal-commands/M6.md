# M6 goal command

~~~text
Goal: perform M6 only if the externally custodied sealed corpus and blind
grader are available; otherwise document the blocker and stop.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md,
docs/evaluation/frontier-agent-ablation-protocol.md, and
docs/goals/frontier-agent-command-refinement/{README.md,M6-confirmatory-study-and-paper.md}.
Act as a rigorous computational-chemistry expert: be transparent about facts,
failures, uncertainty, and limitations; treat advisors as bounded evidence
contributors; never transform a benchmark result into an unsupported SOTA or
autonomous-discovery claim.

Verify the M5 freeze receipt, preregistration, external-custodian independence,
sealed corpus digest, blind grader, and all user-approved current-quota/API
limits before unsealing. After unsealing, do not alter model, prompts, skills,
tool schema, CLI schema, compiler, validators, projects, budgets, task order,
or analysis plan. Run the frozen paired 2 x 2 x 2 command-workflow study; keep
the compiler as command authority in every condition and retain all blocked,
failed, and safety outcomes.

Analyze paired bootstrap confidence intervals, mixed effects, and Holm
correction. Use deterministic graders first, experts second, LLM judges only
as supplemental evidence. Report all per-family results, costs, latency,
approval integrity, false passes/rejections, exclusions, and negative results.
Promote only components meeting preregistered gates to opt-in candidates;
retain the single-agent reference path.

Prepare an internal reproducibility package and conservative paper draft with
architecture, methods, evidence, limitations, and no SOTA language unless a
separately authorized comparison proves it. Do not publish, release data,
submit a preprint/manuscript, run real chemistry, or top up APIs. Use
apply_patch; run only preregistered checks; Conventional Commit and non-force
fork push, no PR. Use the question tool when available; otherwise ask the user
directly and stop only for material unresolved authority.
~~~
