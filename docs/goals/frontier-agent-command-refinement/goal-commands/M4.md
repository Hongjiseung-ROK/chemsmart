# M4 goal command

~~~text
Goal: complete M4, a safe command-DAG workflow and archived scientific evidence
slice for ChemSmart.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, and
docs/goals/frontier-agent-command-refinement/{README.md,M4-command-dag-and-archived-slice.md}.
Act as a rigorous computational-chemistry expert: state uncertainty, learn
from failed gates, and use advisors only with immutable typed packets,
independent work, narrow budgets, and deterministic joins.

Inspect the current compiler, Runtime V2, receipts, artifact store, task-graph
contracts, archived fixtures, reports, and focused tests. Implement a
multi-command DAG in which each node owns one canonical ChemSmart command and
downstream -f-style bindings resolve only from a real predecessor artifact
receipt and hash. Missing predecessors remain planned. Do not manufacture
native input or guessed paths.

Allow subagents only for independent branches such as separately computable
species. They return typed CommandNode IR only; the coordinator recompiles
against live schema and performs deterministic merge. One mutable project or
artifact has one owner. Connect archived Gaussian/ORCA/xTB artifacts to task,
command, evidence, validation, claim, review, and report manifests, preserving
native records, QCSchema-compatible structure, and RO-Crate-compatible
provenance. Cover auxiliary CLI families as command-schema compatibility, not
automatic scientific suitability. Keep D/E/C factors experimental and critics
read-only.

No new engines/HPC/training/API calls. Run one focused DAG/artifact/evidence
suite after M4 is complete, with at most one evidence-driven rerun. Use
apply_patch, Conventional Commit, non-force fork push, no PR. Ask via question
tool only for material unresolved authority.
~~~
