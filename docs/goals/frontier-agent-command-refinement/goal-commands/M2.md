# M2 goal command

~~~text
Goal: implement M2, ChemSmart's typed CommandWorkflowSpec v1 and deterministic
command compiler.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, and
docs/goals/frontier-agent-command-refinement/{README.md,M2-command-workflow-compiler.md}.
Work as an evidence-led computational-chemistry expert; state facts,
assumptions, failures, and limits plainly. The future paper ambition never
permits a SOTA/execution claim from a preview.

Inspect the live branch, Runtime V2 contracts, CLI schema/pruning, compact-v8
adapter, semantic gate, safe preview, independent parser, project settings, and
focused tests. Keep compact-v8 as a migration reader only. Implement canonical
typed ScientificTaskSpec, CommandWorkflowSpec, CommandNode, ArtifactBinding,
CanonicalCommandInvocation, CommandCounterexample, and preflight receipt
contracts as additive, replay-safe interfaces.

Compiler order is fixed: typed IR -> DAG check -> live Click path/option
resolution -> trusted project/artifact grounding -> canonical long-flag argv ->
safe rendering -> fake/test CLI preview -> independent parser observation ->
semantic round-trip. Models may propose JSON IR or at most two structured
repairs; they may not write shell/native inputs, paths, aliases, quoting, or
fallback engine input. Project YAML owns method/basis/ECP/solvent; missing
expressible intent becomes clarification or infeasible, never native fallback.
Reject injection, unknown/out-of-scope options, stale bindings, and changes to
explicit program, geometry, charge, multiplicity, method, or constraints.

Implement only the deterministic A0/A1 shadow comparator with fixed fixtures
or captured M1 traces; reserve live DeepSeek paired trials, token/cost metrics,
and any efficacy claim for M5's frozen pilot. No real engine/HPC/training/API
work. Avoid frequent pytest/Ruff; run one focused compiler suite at M2
completion and one evidence-driven rerun at most. Use apply_patch,
Conventional Commit, non-force fork push, no PR. Use the question tool when
available; otherwise ask the user directly and stop only for a material
unresolved authority, scientific, or branch-ownership choice.
~~~
