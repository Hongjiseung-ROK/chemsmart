# M1 goal command

~~~text
Goal: complete M1, the truthful command baseline and constrained DeepSeek
validation, for ChemSmart.

Read AGENTS.md, docs/design/chemsmart-agent-ultimate-goal.md, and
docs/goals/frontier-agent-command-refinement/{README.md,M1-command-baseline-and-provider-validation.md}.
Adopt the computational-chemistry expert persona: uphold integrity, expose
uncertainty and failures, and collaborate only through bounded typed advisor
work. Never claim SOTA, execution, or reproducibility from a preview.

Inspect the current branch, dirty state, Click schema, compact signatures,
schema pruning, direct-string synthesis, v8 adapter, semantic gate, safe
preview, model parser, provider adapter, AgentSession, UnifiedSessionRunner,
ToolLoop, registry, permissions, and focused tests. Freeze a baseline that
separates implemented, mocked, feature-flagged, and absent behavior.

Implement or verify the command-only frontier profile: project/workspace read,
render, validate, write plus synthesize, bounded repair, inspect, and explain.
Legacy molecule/settings/job/input/execution builders may stay only in an
explicit harness_jobs compatibility profile and must fail closed elsewhere.
Wire official DeepSeek through the real session/runner/loop, not a standalone
probe. For tool calls to official api.deepseek.com only, use the documented
thinking-disabled compatibility behavior; preserve other providers.

Borrow DeepSeek only from a standard Keychain lease. Check liveness and current
quota sufficiency without logging a key, header, raw response, hidden reasoning,
or billing data; never top up. On missing/invalid/insufficient access, record a
non-secret blocked status. Use at most one zero-engine fake/test command
preview. No real engines/HPC/training.

Use inspection and receipts while editing; run one focused M1 suite only when
complete and one evidence-based rerun at most. Use apply_patch, make a
Conventional Commit, non-force push to fork if unchanged, and open no PR.
Use the question tool when available; otherwise ask the user directly and stop
only for material unresolved authority.
~~~
